# Шаг 0035. Отчёт отвечает на свой вопрос один раз — шаги 3–7

> **Редакция 3 (26.08.2026).** Владелец принял все рекомендации; открытых вопросов в плане нет —
> раздел «Принятые решения» ниже. План разбит на пять коммитов со сквозной нумерацией шагов
> **3, 4, 5, 6, 7** (шаги 1–2 ведёт параллельный план). Редакция 1 писалась без артефактов прогона
> и в двух местах ошиблась; что именно опровергнуто — в разделе «Что бандл опроверг».
> Всё в «Контексте» помечено: **измерено** или **гипотеза**.

## Задача и границы

Четыре находки аудита тимлида по живому кейсу Кремлёва (отчёт `rendered-client (6) (1).pdf`,
72 страницы) плюс дефект, найденный в бандле по ходу разбора.

В объёме:

- **находка 3(а)** — заголовок «ТОП-20: позиции 1–8» называет два числа (шаг 3);
- **находка 7** — два дословно совпадающих пункта «почему выделено» на стр. 59 (шаг 4);
- **находки 5 и 4** — заголовок и тело страницы изображений считают разное; оценка строки выдачи
  расходится на одинаковых материалах (шаг 5, одна причина);
- **находка 6** — тема без опоры на стр. 22, цитата не по теме на стр. 52 (шаг 6);
- **обрезанная строка комплаенс-сводки на стр. 69** — предупреждение выставлено, прогон доехал
  до `REPORT_READY` (шаг 7).

Явно вне объёма:

- находки 1 и 2 аудита (посторонний Кулебакин; запрос «Назарович Умар» без фамилии) — параллельный
  план;
- находка 3(б) — почему позиции 9–20 потеряны при сборе;
- расширение словаря `adversePatterns` — решение владельца: не в этом шаге (см. ниже);
- срезание хвоста-бренда площадки в заголовке выдачи («… – The Investigator News») — решение
  владельца: не срезать;
- два словаря тем (`THEME_RULES` против `config/finding-themes.ts`): на снимке ОАЭ рядом стоят
  «PEP / RCA / watchlist-сигналы» и «Сигналы PEP / RCA» — одна тема двумя именами. Отдельный шаг;
- как материал попадает в корзину темы (`themesFor`) — слово совпадения по бандлу не установлено;
- `identity.ts:519`, `related.ts:83`, `suggestions.ts` — свои предикаты негативности. Они
  размечают **строки запроса** и заголовки на другой вопрос; у запроса нет ни домена, ни
  прочитанной страницы, словарь по тексту там единственно возможный ответ. Не трогаем.

## Принятые решения владельца (26.08.2026)

Дословно: «принимаю все рекомендации». По каждому вопросу принят рекомендованный вариант;
обоснование сохранено, потому что именно оно защищает решение от «упрощения» обратно.

1. **Заголовок неполной таблицы — «Россия — Google, позиции 1–8 из ТОП-20 (1/2)».** Знаменатель
   клиенту нужен: «позиции 1–8» без него неотличимы от полной выдачи из восьми строк. Печатается
   вычисленный диапазон (`min…max`), а не количество: на наборе {4,6,7,8,9,10} «собрано 6» и
   «позиции 4–10» — разные факты, и печатать надо тот, который вычислен.
2. **Колонка «Оценка» означает собственный сигнал материала**, а не принадлежность к теме
   повышенного внимания. Так называется колонка, так работает красная рамка на снимке, и klerk.ru
   («страница прочитана, вердикт `supportive`, две цитаты») перестаёт быть «Нежелательной».
   Принадлежность к теме остаётся языком тем и матрицы рисков.
3. **Строка, чью страницу не читали и у которой нет негативного сигнала, печатается
   «Не проверено».** Сегодня 14 из 38 строк отчёта печатают «Нейтральный» там, где не проверялось
   ничего, и клиент не может их отличить от проверенных.
4. **`adversePatterns` в этом шаге не трогаем.** Слов «обвин», «нападени», «избие» в словаре нет,
   и обе строки про «Сульянов обвинил в нападении» сойдутся на «Не проверено» / «Нейтральный».
   Причина отсрочки: словарь читают ещё синтез находок и уровни риска, добавка двигает всю матрицу
   и смешала бы два изменения в одном диффе.
5. **Хвост-бренд площадки не срезаем, домен предикатом не читаем.** На стр. 62 «негативный
   заголовок» — это «**Investigat**» внутри «The **Investigat**or News». Предикат читает заголовок
   и сниппет; домен отвечает отдельным именованным списком. Так ложное срабатывание остаётся, но
   не удваивается.
6. **`ruwiki.` и `wikiwand.` добавляются в мягкие домены** (`SOFT_PROFILE_DOMAIN_RE`). Иначе
   ru.ruwiki остаётся «Нежелательным» по слову «скандалы» в заголовке, а ru.wikipedia оценивается
   по прочитанной странице — энциклопедии снова разъезжаются.
7. **Тема без несущей цитаты в регионе печатает честную пустую ветку** («По теме в источниках …;
   отдельный заголовок с сутью риска в выдаче не выделен — сверить первоисточники»), уже
   реализованную в `shared.ts:1495`.

## Что бандл опроверг

Прямо, а не тихой переписью:

1. **Находка 7 диагностирована неверно.** Было: «два разных наблюдения одного домена дают
   одинаковый текст, потому что во фразе нет адреса». Измерено: это **один и тот же адрес**
   `https://www.opensanctions.org/entities/Q55102113/`, позиция 2, GOOGLE/UAE, попавший в видимые
   строки снимка дважды под двумя ref-ами, различающимися только запросом. Адрес во фразе не
   изменил бы ничего.
2. **Находка 6(б) чинилась не в том файле.** Было: `resolveExampleQuote` в
   `finding-synthesizer.ts`. Измерено: цитату на стр. 52 поставил `localizedThemedClaim`
   (`shared.ts:1360`).
3. **Находка 6(а): в напечатанном отчёте причина была третья.** Пак прогона нёс опору темы, а до
   клиента она не доехала — шаблон печатал два предложения. Это уже исправлено коммитом `ef8bdb6`
   (v116 → v117). На HEAD тема снова без опоры, но уже по решению владельца, а не по потере.
4. **«Одна статья Википедии на двух языках, разные оценки» — не дефект.** Испанская статья
   прочитана и признана нейтральной (2 цитаты, биографический зачин), французская прочитана и
   признана нежелательной (2 цитаты, AIBA не признаётся МОК с июня 2023). Это разные тексты и два
   честных вердикта. **Строка оставлена в плане намеренно: через месяц это единственная защита от
   того, что кто-нибудь «починит» честную оценку обратно.** Ни один шаг этого поведения не меняет.
   То же про `ru.wikipedia.org` (стр. 25, строка 1): «Нежелательный» стоит по прочитанной странице
   с дословной цитатой «Имел судимости за вымогательство (2004) и нанесение побоев (2007).» —
   претензия аудита к этой строке не подтвердилась.

## Контекст

### Прогон, по которому всё измерено

`bundle-manifest.json`: `unified-1787645589961-ae90a7ba`, `stage: REPORT_READY`,
`status: COMPLETED`. Паки секций собраны версией **`deck-sections-v116`**
(`deck/section-packs/ru/serp.json` → `contentVersion`); HEAD — `v117`, разница ровно в коммите
`ef8bdb6`. `analytics/link-verdicts.json`: `requested 120, readOk 90, failed 30`
(`blocked 16`, `empty_text 11`, `not_fetched 3`), `summary.adverse 20`.

### Шаг 3: заголовок называет два числа — измерено

`fragment-builders/serp.ts:677–686`:

```ts
const range =
  table.positional && missing
    ? `: позиции ${Math.min(...printedRanks)}–${Math.max(...printedRanks)}`
    : "";
const depth = table.positional ? `, ТОП-${SERP_TABLE_TOP_N}${range}` : ": собранная выдача";
```

Замысел назван в комментарии на 668–676 и честен; читается результат склейки. Абзац под таблицей
(«Позиции 9–20 … потеряны при сборе») верен и не меняется.

**Ни один эталон эту ветку не исполняет**: у золотого кейса таблицы полные (`missing` пуст), у
`report-72` — непозиционные («собранная выдача»). Единственный сторож формулировки —
`tests/unit/serp-table-caption-tells-collected-range.test.ts:137,143`, и он закрепляет сегодняшнюю
строку дословно.

### Шаг 4: снимок считает наблюдения, а не материалы — измерено

`deck/section-packs/uae/serp-screenshot.json`, слайд `p27_uae_serp_visual`: пять объяснений.
`visual-assets-by-slot.json`, тот же слот: пять видимых строк, все `adverse: true`. Разрешение
ref-ов в наблюдения (`analytics/composite-serp-observations.json`):

| ref | url | rank | `observationKey` |
|---|---|---|---|
| `obs-b6940f3cd96baaf6` | `opensanctions.org/entities/Q55102113/` | 2 | `umar nazarovich kremlev\|GOOGLE\|UAE\|organic\|…` |
| `obs-2db89b8102559fdc` | **тот же** | 2 | `nazarovich umar\|GOOGLE\|UAE\|organic\|…` |
| `obs-9ce6da58bfcaeb0e` | `rucriminal.info/en/dosje/125` | 3 | `umar nazarovich kremlev\|…` |
| `obs-10ddec90f4146568` | **тот же** | 3 | `nazarovich umar\|…` |
| `obs-a26723b15f07e95b` | `rupep.org/en/person/49596` | 7 | — |

Ключ наблюдения включает запрос — намеренно (`serp.ts:176`). Таблица выдачи это учитывает и
склеивает материал (`mergeSerpRowsByMaterial`, `serp.ts:322–339`, ключ `домен|заголовок`).
**Снимок — нет:** `adverseVisualSidebar` защищается только от повтора ref (`shared.ts:3040`).

Следствия на одной странице: «На снимке выделено 5 результатов» при трёх материалах; две лишние
красные рамки; продолжение «почему выделено» разошлось на две страницы (3 + 2) вместо одной.

### Шаг 5: «негативна ли строка» отвечают пять раз — измерено

`docs/ENGINEERING.md` §8 (строка 1776) утверждает, что ответ один и фолбэк один. **Код с этим
разошёлся.**

| Где | Что решает | Как считает |
|---|---|---|
| `serp.ts:540–597` (`rowOf`) | колонку «Оценка» | `readVerdictTone` (без требования цитаты) → иначе `ev.adverse` **или** `adverseRefSet` **или** `ADVERSE_PATTERNS` по **заголовку** |
| `resolve-observation-highlights.ts:163–209` | рамку и `visibleItems[].adverse` | вердикт (`adverse` **с цитатой**) → `ADVERSE_DOMAIN_RE` → `STRONG_ADVERSE_BLOB_RE`/`WEAK_ADVERSE_BLOB_RE` по «заголовок + сниппет + url + домен», с защитой мягких доменов |
| `shared.ts:848–889` (`composePageRowComposition`) | «негативных заголовков — N» в теле | та же тройка, но `readVerdictTone` не смотрит вовсе |
| `images.ts:258–292` | заголовок страницы изображений | плитки + непоказанные строки, то есть предикат №2 |
| `canonical-visual-assets.ts:833–838` | рамку плитки и `notShown[].adverse` | предикат №2, **без `verdictByRef`** — второй аргумент не передан, хотя строкой 863 `toVisibleItem` его передаёт |

Словари не совпадают ни в одну сторону (`config/finding-themes.ts:201` против
`resolve-observation-highlights.ts:22–29`). Только в `adversePatterns`: `суд/court`, `прокур`,
`отмыв`, `обыск`, `розыск`, **`скандал`**, `расследован/investigat`, `спецслужб/фсб`. Только в
blob: `нежелат/негативн`, `ofac`, `lawsuit`, `pep`, `rca`, `offshore/офшор`, `бенефициар`,
`молдав`, `лнр`, `rucriminal/rupep/opensanctions`.

**Стр. 35 — измерено.** Слот `p14_ru_images_1`: пять плиток, у всех `adverse: false`; одна строка
без превью (`not_an_image`), тоже `false` → `adverseTotal = 0` → заголовок «негативных источников
нет». Тело нашло одну: `ru.ruwiki.ru`, заголовок «Кремлёв Умар Назарович: биография, бизнес,
**скандалы**» — совпадение `"скандал"` (прогонял скомпилированное выражение по всем пяти
заголовкам слота, больше совпадений нет). Слова «скандал» нет ни в `STRONG_ADVERSE_BLOB_RE`, ни в
`WEAK_ADVERSE_BLOB_RE`, а `ru.ruwiki.ru` не покрыт `SOFT_PROFILE_DOMAIN_RE` (там только
`wikipedia\.`).

**Стр. 62 — измерено, и здесь неправа другая сторона.** Слот `p30_uae_images`: четыре плитки, все
`false`; две строки без превью, обе `false`. Тело нашло одну:
`theinvestigatornews.com`, заголовок «… – The **Investigat**or News» — совпадение `"Investigat"`
внутри названия площадки в хвосте заголовка. То есть на стр. 35 ошибается заголовок страницы, а
на стр. 62 — тело; «выбрать одну сторону» недостаточно.

**Находка 4 построчно — измерено по `analytics/link-verdicts.json`:**

| Строка | Оценка | Что её объясняет |
|---|---|---|
| стр. 25/1 `ru.wikipedia.org` | Нежелательный | вердикт `adverse`, 3 цитаты. **Верно** |
| стр. 56/10 `fr.wikipedia.org` | Нежелательный | вердикт `adverse`, 2 цитаты. **Верно** |
| стр. 56/9 `es.wikipedia.org` | Нейтральный | вердикт `neutral`, 2 цитаты. **Верно** |
| стр. 20/2 `rbc.ru` | Нейтральный | **`readFailure: "blocked"`** — вердикта нет, словарь по заголовку, слов «обвин»/«нападени» в `adversePatterns` нет |
| стр. 22/10 `uz.kursiv.media` | Нежелательный | вердикт `adverse`, 2 цитаты |
| стр. 22/11 и 27/7 `klerk.ru` | Нежелательный | принадлежность к находке «Офшоры» (`medium`) — при вердикте **`supportive`** с двумя цитатами |

- **rbc.ru против uz.kursiv.media — это «не прочитано» против «прочитано».** Один сюжет, но одну
  страницу открыли и признали нежелательной с цитатами, а вторую площадка не отдала. В отчёте обе
  стоят одним словом.
- **klerk.ru — дефект, и он объяснён.** Абзац стр. 22 из пака: «Материалы по теме на этой
  странице — globalmsk.ru и **klerk.ru**». Эта строка строится из `view.supportDomains` находки
  «Офшоры / корпоративное владение», у которой `riskLevel: "medium"` → `isAdverse(f)` истинно
  (`shared.ts:201`) → её `evidenceRefs` попадают в `adverseRefSet`. В `rowOf` это второй дизъюнкт,
  и вето прочитанной страницы его не снимает: `readVerdictTone === "supportive"` возвращает `false`
  **только для своего ref**, а `group.refs.some(...)` красит материал по любому другому ref той же
  строки. *Гипотеза, не доказанная поref-но:* ref, которым находка цитирует klerk
  (`inventory:obs-180d652c6130abe5`), в `analytics/composite-serp-observations.json` отсутствует, и
  разрешить его в URL по бандлу нельзя. Механизм доказан напечатанной строкой опоры, ref — нет.

**Два числа, которых в аудите не было.** Сверка всех строк таблиц выдачи
(`deck/section-packs/{ru,uae}/serp.json`, сопоставление `rowAddresses` с `link-verdicts`):

- напечатано **38** строк;
- **14 из них — «Нейтральный» при непрочитанной странице**: 9 с отказом чтения, 5 без вердикта
  вовсе;
- **`opensanctions.org/entities/Q55102113` в таблице ОАЭ, позиция 2, напечатана «Нейтральный»** —
  при том что эта же страница прочитана и признана `adverse` с тремя цитатами. Вердикт записан на
  ref `inventory:obs-195bfa41636212ef` (запрос «умар назарович кремлев реестр», регион RU), а
  строка ОАЭ собрана из своих одиннадцати ref-ов. **Вердикт привязан к наблюдению, строка — к
  материалу, и они не встречаются.** Тот же URL двумя страницами дальше несёт красную рамку.

Для полноты: на снимке `p10_ru_serp_visual` красных **10 из 10** строк — включая «Умар Кремлев
стал владельцем "Рольфа": детали сделки» (kommersant.ru) и интервью на championat.com. Отдельной
находкой не заявлено, но дифф шага 5 надо читать и по этой странице.

### Шаг 6: тема без опоры и цитата не по теме — измерено

**(а)** В паке прогона (`deck/section-packs/ru/serp.json`, «Россия — Яндекс, ТОП-20 (3/5)»):
`narrative` содержит «… «Офшоры / корпоративное владение» — средний уровень внимания. **Материалы
по теме на этой странице — globalmsk.ru и klerk.ru.** Выдача проверена по 5 запросам: …».
В `deck/assembled-deck.json` — та же строка целиком. **В PDF её нет**: `grep` по всем 72 страницам
даёт 0 вхождений «Материалы по теме на этой странице» и 0 вхождений «Выдача проверена по».
`render/layout-telemetry.json`: `orion_text_body_p22`, `textLength: 114`, `clipped: false` — до
рендерера доехали первые 114 знаков, и он честно отчитался, что уложил всё, что получил.
Исправлено коммитом `ef8bdb6`. Тем же коммитом, по решению владельца, перечень доменов из абзаца
убран: полосы адресов печатают те же домены целиком и все. Следствие: **на HEAD страница печатает
тему и ничего больше.** Это же подтверждает выбор опоры — домены отвергнуты именно за спор с
полосой адресов, номер строки не спорит.

**(б)** Находка `finding-financial_claims-subject_match-f861cec8`, `riskLevel: "low"`, два ref-а.
Её глобальный `claim` называет обе опоры: `kulak.team/…-millioni-dolgov` («Миллионы **долгов**
Hardcore» — настоящая, слово темы есть) и Instagram-подборку. Первая RU-регионная и на странице
ОАЭ отфильтрована (`shared.ts:1386`). Остаётся Instagram, и его цитатой становится не заголовок, а
`pageQuote` из вердикта: `fromPage` весит 12 против максимум 10 у заголовка (`shared.ts:1440–1446`).
Вердикт этого ref-а: `tone: "neutral"`, `theme: "Подборка популярных роликов Instagram об Умаре
Кремлеве"` — отсюда строка «О чём».

Защита от ровно этого случая **есть** (`shared.ts:1401`):

```ts
if (adverseTheme && (e.readVerdictTone === "neutral" || e.readVerdictTone === "supportive")) continue;
```

и не сработала, потому что `adverseTheme = isAdverse(f)` требует `riskLevel >= medium`, а тема
`low`. Причина защиты — «тему назначил словарь по заголовку, а страница прочитана» — от уровня
риска не зависит.

Второе: строка счёта берётся из глобального claim'а дословно (`shared.ts:1474`) и печатается рядом
с региональными цитатами. Отсюда «Всего по теме: 2 материала» над одной цитатой.

*Гипотеза, не закрытая:* каким словом Instagram-подборка попала в корзину `financial_claims`.
`themesFor` сверяет ключевые слова с «заголовок + сниппет + classification + **sourceUrl**»
(`finding-synthesizer.ts:143–147, 725–733`); ни напечатанный текст, ни адрес совпадения не дают.
Полного сниппета в бандле нет.

### Шаг 7: обрезка комплаенс-сводки — измерено

`render/layout-telemetry.json`: `orion_search_table_p69`, `availableHeight 4 602 385`,
`requiredHeight 4 627 880`, `measuredLines 21`, `clipped: true`. В предупреждениях прогона —
`renderer-clip:page=69:TABLE_ROW_PARTIALLY_VISIBLE`, и прогон доехал до `REPORT_READY`. На той же
странице подпись ячейки «Также числится как» напечаталась как «Также числится».

Причина переполнения названа в коде: `fragment-builders/compliance.ts:89`,
`const RECORDS_PER_PAGE = 2` — **ёмкость считается в записях, а строк у записи от 6 до 10**
(`compliance.ts:207–231`: имя, категория, статус, оценка, уверенность, алиасы, страны, даты,
сводка, карточка). На стр. 69 две записи дали 18 строк + 2 полосы-заголовка + шапку = 21.

Ворота телеметрии судят это намеренно мягко: `services/render-telemetry-gate.ts:17–28` объявляет,
что блокер — только `CONTENT_DROPPED_BY_RENDERER`, а клип это «защита сработала», и блокировка по
нему остановила бы здоровые прогоны (мера `clipped` консервативна, запас ×1,18). Правило верное для
текстовых путей и **неверное для комплаенса**: там обрезается не хвост предложения, а строка
карточки записи, которую продукт обещает передать аналитику целиком.

Второе предупреждение бандла, `empty-state-slides:2`, **дефектом не является**:
`report-quality-summary.json` называет оба слайда — `p34_dow_jones` и `p35_lexis_visual`, причина
`compliance-check-not-performed`. Это честная пустота неподключённой базы.

## Решение

Общий принцип: **страница отвечает на свой вопрос один раз и называет, на чём ответ стоит.**

### Шаг 3 — заголовок таблицы называет диапазон один раз

`, ТОП-20: позиции 1–8` → `, позиции 1–8 из ТОП-20`. Полная позиционная таблица — как сегодня
(`, ТОП-20`), непозиционная — как сегодня (`: собранная выдача`).

Выражение выносится в чистую функцию `serpTablePageTitle` в том же `serp.ts` — не ради красоты, а
потому что эталоны эту ветку не исполняют и закрепить формулировку можно только юнитом.

### Шаг 4 — снимок выдачи считает материалы, а не наблюдения

Видимые строки снимка сводятся по **материалу** тем же ключом, каким это делает таблица
(`serpMaterialKey`), **до** того как считаются рамки, объяснения и число «выделено N». Один
материал — одна рамка, одно объяснение, одна единица счёта.

Почему не адрес во фразе (редакция 1): адреса у дублей одинаковы. Почему не дедупликация по
итоговому тексту: она снимает объяснение с рамки, а рамок остаётся столько же — лечится след, а не
причина.

Что должно пережить план в `ENGINEERING.md`: **ключ наблюдения включает запрос, поэтому любая
поверхность, показывающая строки выдачи, обязана сводить их по материалу.**

### Шаг 5 — один предикат, вердикт по материалу, значение «Не проверено»

**(а) Один предикат.** Одна экспортируемая функция в `resolve-observation-highlights.ts` (модуль,
который уже объявляет себя единственным ответом), порядок решений:

1. вердикт прочитанной страницы: `subjectMatch === "other"` → нет; `neutral|supportive` → нет;
   `adverse` **с цитатой** → да; `adverse` без цитаты → падаем в словарь;
2. `ADVERSE_DOMAIN_RE` по url/домену → да;
3. `ADVERSE_PATTERNS` (конфиг) по **заголовку и сниппету**, кроме мягких доменов → да;
4. иначе нет.

`STRONG_ADVERSE_BLOB_RE` и `WEAK_ADVERSE_BLOB_RE` удаляются: словарь негатива один и лежит в
конфиге. Из `rowOf` удаляется `adverseRefSet` — принадлежность к теме есть тема, а не оценка
материала; это уже написано в комментарии `serp.ts:547–559` и не выполнено в коде. Словарь читает
заголовок и сниппет и **не читает url и домен** — решение владельца №5.

`SOFT_PROFILE_DOMAIN_RE` пополняется `ruwiki\.` и `wikiwand\.` — решение владельца №6.

**(б) Вердикт привязывается к материалу.** Карта вердиктов раскладывается по ключу материала, а не
только по `evidenceRef`; `readFailure` — так же. Без этого пункт (а) бесполезен на главной строке
отчёта: OpenSanctions прочитан, признан нежелательным с тремя цитатами и всё равно печатается
«Нейтральным».

**(в) «Не проверено» — своё значение колонки.** Порядок значений сверху вниз:

```
О другом лице  →  Нежелательный  →  Вероятно  →  Не проверено  →  Нейтральный
```

«Не проверено» ставится, когда **вердикта по материалу нет** (страница не читалась или чтение
отказало) **и** негативного сигнала нет. Непрочитанная строка с негативным сигналом остаётся
«Нежелательной»: рамку на снимке она получает по тому же правилу, и глушить сигнал из-за того, что
страницу не открыли, — потеря. Неопределённость принадлежности («Вероятно») стоит выше
непрочтения: это более сильная оговорка.

Признак берётся из данных, а не из слова статуса: **нет вердикта — не проверено.** Условия «а
выполнялась ли вообще стадия чтения» нет намеренно — иначе прогон без чтения печатал бы
«Нейтральный» на строках, которых никто не открывал, то есть вернулся бы ровно тот дефект, который
шаг закрывает. Цена названа в «Рисках» и она велика.

**(г) Заголовок и тело страницы изображений считают один набор.** `composePageRowComposition`
получает второй, необязательный набор ссылок («найдено, но не нарисовано»); `shown` остаётся
числом нарисованных, негатив считается по объединению; заголовок берёт то же число.
`canonical-visual-assets.ts:833` получает `input.verdictByRef`.

**(д) Рендерер узнаёт новое значение.** `_status_tone` (`renderer/orion_golden_render/visual.py:568`)
незнакомое слово красит серым — тем же цветом, что «Нейтральный», и различие пропало бы. «Не
проверено» — незакрытый вопрос, а не риск и не благополучие, то есть ровно тот случай, для
которого в функции уже заведена янтарная ветка («Не подтверждено», «статус не зафиксирован»).
Подсветка строки (`visual.py:885`) не меняется: янтарной остаётся только `likely`.

### Шаг 6 — тема называет свою опору, цитата несёт свою тему

**(а)** Тематический вывод на странице выдачи дополняется опорой — **номерами строк этой
страницы**: «"Офшоры / корпоративное владение" — средний уровень внимания; на этой странице —
строки 9 и 11.» Домены не возвращаются (решение владельца 25.08). Если ни одна напечатанная строка
тему не поддерживает, тематический вывод **не печатается** — печатается описание состава страницы
(`pageRowCompositionBlocks`, ветка уже существует).

**(б)** Условие `adverseTheme` снимается с защиты `shared.ts:1401`: материал, чью страницу
прочитали и признали нейтральной или благоприятной, не цитируется доказательством темы — **любой**
темы. Причина защиты от уровня риска не зависит ни в чём.

**(в)** При региональной пересборке строка счёта пересчитывается по тем же материалам, из которых
собраны цитаты, а не переносится из глобального claim'а.

### Шаг 7 — обрезка карточки комплаенса не доезжает до клиента

Две части, и обе нужны:

**(а) Ёмкость считается в строках, а не в записях.** `RECORDS_PER_PAGE = 2` заменяется отбором по
фактическому числу строк записи: на страницу идёт столько записей, сколько влезает по бюджету
листа. Это устраняет причину.

**(б) Клип на странице комплаенса — блокер.** Общее правило («клип — предупреждение») остаётся:
оно верно и обосновано в `render-telemetry-gate.ts:17–28`. Исключение узкое и названное:
`TABLE_ROW_PARTIALLY_VISIBLE` **на странице комплаенс-раздела** блокирует выдачу, потому что там
обрезается строка карточки записи, а карточка — печатный носитель правила «совпадение уходит
аналитику целиком». Страница узнаётся по имени фигуры телеметрии и разделу слайда, а не по номеру
страницы.

Шаг идёт **после шага 2 параллельного плана**: там с этой же таблицы снимаются четыре строки
(«Оценка совпадения», «Уверенность сопоставления» и соседние), и переполнение может исчезнуть само.
Задача от этого не исчезает: ворота нужны и на будущих записях с длинными алиасами.

## Порядок и разбиение на шаги

Пять коммитов, каждый с зелёным `npm run ci`, своим диффом эталонов и своим решением по версии.

| № | Шаг | Находка | Эталоны | `DECK_CONTENT_VERSION` |
|---|---|---|---|---|
| 3 | Заголовок неполной таблицы выдачи | 3(а) | **не двигает ни один** | **поднять** |
| 4 | Сведение видимых строк снимка по материалу | 7 | золотой кейс; `report-72` — проверить | **поднять** |
| 5 | Единый предикат + вердикт по материалу + «Не проверено» | 5 + 4 | оба, широко | **поднять** |
| 6 | Тема и цитата (`localizedThemedClaim`) | 6(а) + 6(б) | оба | **поднять** |
| 7 | Обрезка страницы комплаенса — блокер выдачи | — | `report-72` — проверить | **поднять** |

Шаг 3 первым: эталоны не двигает, дифф читается за минуту. Шаг 4 перед шагом 5 намеренно — он
вводит понятие «одна строка — один материал» на снимке, а шаг 5 пользуется тем же понятием,
привязывая к материалу вердикт. Шаг 5 идёт отдельным коммитом на чистом фоне: у него самый большой
дифф клиентского текста во всём плане. Шаг 6 после шага 5 — оба правят один абзац страницы выдачи.
Шаг 7 после шага 2 параллельного плана.

**Подъём версии нужен в каждом шаге без исключений.** Исключение «правка не может изменить ни одну
собранную деку» здесь не действует ни разу: шаги 3–6 правят файлы в `fragment-builders/`, шаг 7 —
`compliance.ts` там же. В шаге 5 сторож отпечатка вдобавок **не увидит** правку
`load-deck-inputs.ts` (файла нет в списке `EXTRA_SOURCES` теста
`tests/unit/deck-content-version.test.ts`) — версию там поднимать руками.

## План реализации

Формат каждого шага: **красная фаза → файлы → признак зелёного → эталоны → версия**. Номера
поведений в скобках отсылают к разделу «Что должно быть протестировано».

### Шаг 3. Заголовок неполной таблицы выдачи

**Красная фаза.** Новый юнит в `tests/unit/` на поведения 1–4. Пишется до кода, красный лог
сохраняется файлом.

**Файлы.**

1. `fragment-builders/serp.ts` — вынести сборку заголовка (677–686) в экспортируемую чистую
   функцию `serpTablePageTitle({ region, engineLabel, positional, printedRanks, missing, suffix })`.
   Полная позиционная → `<Регион> — <Движок>, ТОП-20<суффикс>`; неполная →
   `<Регион> — <Движок>, позиции <min>–<max> из ТОП-20<суффикс>`; непозиционная →
   `<Регион> — <Движок>: собранная выдача<суффикс>`; ветка без названия движка — та же подстановка.
2. `tests/unit/serp-table-caption-tells-collected-range.test.ts` — правка двух ожиданий (строки
   137 и 143). **Это правка теста после красной фазы; называется отдельной строкой в отчёте
   разработчика.**
3. `deck-sections/content-version.ts` — версия и отпечаток.

**Признак зелёного.** В `buildSerpFragment` осталась одна строка вызова; формулировка существует
ровно в одном месте; подстроки `ТОП-20: позиции` в исходниках нет.

**Эталоны.** Не двигаются. `fixtures/golden-case/client-text.baseline.json` и
`baselines/report-72/` обязаны остаться байт-в-байт: у золотого кейса таблицы полные, у `report-72`
непозиционные. **Если хоть один эталон сдвинулся — это сигнал, что правка задела не ту ветку.**

**Версия.** Поднять (правится файл в отпечатке).

### Шаг 4. Сведение видимых строк снимка по материалу

**Красная фаза.** Юниты на поведения 5–8. Восьмое (сторож эталона на дубли буллетов) пишется сразу
и на текущем эталоне обязано **упасть** — это и есть доказательство, что дефект в эталоне есть.

**Файлы.**

1. `fragment-builders/shared.ts`, `adverseVisualSidebar` (3010–3065) — свести `visibleRows` по
   ключу материала до вычисления `adverseRows`, `gridRefs` и объяснений. Ключ общий с таблицей:
   `serpMaterialKey` переезжает из `serp.ts` в `shared.ts` либо импортируется — выбрать так, чтобы
   **второй реализации не появилось**, и сказать в отчёте, какой вариант выбран.
2. `fragment-builders/serp.ts` — счётчик «На снимке выделено результатов повышенного внимания: N»
   (903–913) и `metrics.adverseHighlights` берут число после сведения.
3. `content-version.ts` — версия и отпечаток.

**Признак зелёного.** Два ref-а одного URL дают одну рамку, одно объяснение и одну единицу счёта;
два разных URL одного домена остаются двумя строками; число в подписи равно числу объяснений и
числу рамок.

**Эталоны.** `NETWORK_CALLS=0 npx tsx scripts/run-golden-case-report.ts --update-baseline`.
Ожидаемые строки диффа в `client-text.baseline.json`:

- `p27_uae_serp_visual`, буллет 1: «На снимке выделено результатов повышенного внимания: **5**
  (emirates-ledger.ae, abudhabi-capital-news.ae, khaleej-finance-post.ae)» — число меняется;
- там же: «Выделенные материалы (**5**) видны при первичной проверке» — то же число;
- `p27_uae_serp_visual__why1` (сегодня 3 буллета, из них 1 и 2 дословно совпадают) и
  `__why2` (2 буллета, первый совпадает с третьим буллетом `__why1`) — состав и, вероятно, число
  слайдов;
- `slideCount` и `totalChars` в шапке файла.

**Совпадают ли дубли золотого кейса по URL, из текстового эталона не видно** (там нет адресов) —
это проверяется в красной фазе по собранному паку, и результат называется в отчёте. Если окажется,
что `emirates-ledger.ae` — два разных URL, эталон не двинется, и тогда шаг закрывается юнитами, а
владельцу говорится, что на золотом кейсе дефект не воспроизводится.

`baselines/report-72/` — проверить `p27_uae_serp_visual` и `p10_ru_serp_visual` и назвать, двинулся
ли эталон.

**Версия.** Поднять.

### Шаг 5. Единый предикат, вердикт по материалу, «Не проверено»

**Красная фаза.** Юниты на поведения 9–21. Особо: 13 (klerk.ru), 15 (opensanctions), 16
(непрочитанная строка) и 12 (стр. 62) пишутся дословными входами из бандла — они и есть аудит,
переведённый в тесты.

**Файлы.**

1. `serp-observation/resolve-observation-highlights.ts` — экспортировать
   `resolveRowAdverse(row, verdict?)` с порядком решений из «Шага 5(а)»; удалить
   `STRONG_ADVERSE_BLOB_RE` и `WEAK_ADVERSE_BLOB_RE`; в `SOFT_PROFILE_DOMAIN_RE` добавить
   `ruwiki\.` и `wikiwand\.`; `classifyObservationHighlight` оставить обёрткой, выбирающей тему.
2. `deck-sections/load-deck-inputs.ts` (541–590) — вердикт и `readFailure` раскладываются по всем
   ref-ам того же материала.
3. `deck-sections/template-registry.ts` — новая константа рядом с `RED_MARKER_LABEL` (164):
   `UNVERIFIED_LABEL = "Не проверено"`; добавить её в `legend` шаблона `serp-table` (282).
4. `fragment-builders/serp.ts`, `rowOf` (540–597) — вызов общего предиката; `adverseRefSet` из
   оценки убран; новое значение в порядке «О другом лице → Нежелательный → Вероятно → Не проверено
   → Нейтральный»; в `metrics` добавить счётчик непроверенных рядом с `adverseDisplayed` (769).
5. `fragment-builders/shared.ts`, `composePageRowComposition` (848–889) — тот же предикат; второй,
   необязательный набор ссылок, учитываемый только в счёте негатива.
6. `fragment-builders/images.ts` (203–292) — передать ссылки `notShownOnPage`; заголовок и тело
   берут число из одного выражения.
7. `services/canonical-visual-assets.ts:833` — передать `input.verdictByRef`.
8. `renderer/orion_golden_render/visual.py`, `_status_tone` (568) — «не провер» в янтарную ветку,
   рядом с «не подтвержд» / «не зафиксирован». Подсветку строки (885) не трогать.
9. **`docs/ENGINEERING.md` — тем же коммитом**, см. «Что переедет в ENGINEERING.md».
10. `content-version.ts` — версия и отпечаток. **Отпечаток не увидит правку
    `load-deck-inputs.ts` — поднимать руками.**

**Признак зелёного.** В `resolve-observation-highlights.ts` не осталось словарей негатива, кроме
списков доменов; заголовок и тело страницы изображений берут число из одного выражения; строка,
чей вердикт снят на другом ref-е того же материала, видит его; строка без вердикта и без сигнала
печатает «Не проверено».

**Эталоны.** Двигаются оба, широко. Что читать вслух:

- **колонка «Оценка» в обоих эталонах**. `ENGINEERING.md` §8 («Заголовок называет то, что
  перечисляет») фиксирует: **решений по ссылкам нет ни у золотого кейса, ни у `report-72`.**
  Значит, после шага все строки без словарного и доменного совпадения станут «Не проверено», а не
  «Нейтральный», — и это будет самое частое слово в таблицах обоих эталонов. Это верно (в фикстурах
  действительно ничего не читали) и **должно быть объявлено владельцу до правки эталона**, иначе
  дифф читается как обвал качества;
- **`p09_ru_serp_table` золотого кейса, строка 2**: `nordmarket-watch.se` «…linked to Malta holding
  structure and offshore beneficial ownership» — сегодня «Нежелательный» по `adverseRefSet`; слова
  `offshore` в `adversePatterns` нет, домена в списке нет, вердикта нет → станет «Не проверено».
  Это прямое следствие решения владельца №2 и должно быть названо отдельно;
- **строка 4 той же таблицы** «Stockholm **court** schedules hearing» — остаётся «Нежелательный»
  (`court` в `adversePatterns` есть). Пара строк 2 и 4 — лучшая иллюстрация того, что шаг делает;
- **заголовки страниц изображений** (`p14_ru_images_1`, `p15_ru_images_2`, `p30_uae_images`) —
  сверить с телом каждой;
- `report-72`: те же три среза плюс счётчики «выделено N» на снимках.

**Версия.** Поднять.

### Шаг 6. Тема называет опору, цитата несёт тему

**Красная фаза.** Юниты на поведения 22–27.

**Файлы.**

1. `fragment-builders/serp.ts` — при нарезке страниц сохранить соответствие «напечатанная строка →
   её ссылки» (сейчас `refChunks[i]` немедленно `.flat()`, строка 653).
2. `fragment-builders/shared.ts` — `PageBlockOptions` получает список `{ rank, refs }` напечатанных
   строк; `pageScopedConclusion` (826–833) дописывает к `themeAttentionLine` фразу с номерами
   поддерживающих строк; при пустом пересечении вызывающий получает `pageRowCompositionBlocks`.
3. `fragment-builders/shared.ts:1401` — снять условие `adverseTheme`.
4. `fragment-builders/shared.ts:1474` — строка счёта в региональной ветке пересчитывается по
   отобранным материалам.
5. `content-version.ts` — версия и отпечаток.

**Признак зелёного.** На странице выдачи тема либо названа вместе с номерами своих строк, либо не
названа вовсе; нейтрально прочитанная страница не цитируется под темой любого уровня риска; счёт
материалов на региональной странице сходится с её цитатами.

**Эталоны.** Двигаются оба. Что читать вслух: абзацы страниц выдачи в
`p09_ru_serp_table*`/`p26_uae_serp_table*` (появятся номера строк либо исчезнет тема) и текст
утверждений тем в `p07_ru_summary*`/`p24_uae_summary*` (у части тем пропадёт цитата и изменится
число «Всего по теме»).

**Версия.** Поднять.

### Шаг 7. Обрезка страницы комплаенса — блокер выдачи

Идёт **после шага 2 параллельного плана.**

**Красная фаза.** Юниты на поведения 28–30.

**Файлы.**

1. `fragment-builders/compliance.ts:89` — `RECORDS_PER_PAGE` заменяется отбором по числу строк
   записи: на страницу идёт столько записей, сколько влезает по бюджету листа. Число строк записи
   считается там же, где она собирается (207–231).
2. `services/render-telemetry-gate.ts` — `TABLE_ROW_PARTIALLY_VISIBLE` на странице
   комплаенс-раздела поднимает блокер; остальные клипы остаются предупреждениями. Комментарий
   17–28 дополняется названным исключением и причиной.
3. `content-version.ts` — версия и отпечаток.

**Признак зелёного.** Запись комплаенса с максимальным числом строк (имя, категория, статус,
алиасы, страны, даты, сводка, карточка) не переполняет лист; прогон с клипом на комплаенс-странице
не доходит до `REPORT_READY` и называет причину.

**Эталоны.** `baselines/report-72/` — проверить пагинацию страниц комплаенса и назвать, изменилась
ли она. Эталон клиентского текста золотого кейса компаенс-записей такого размера не содержит —
проверить и сказать.

**Версия.** Поднять.

## Что должно быть протестировано

Всё обязано проходить **офлайн** — без сети, базы и рендерера (кроме пункта 20, который читает
исходник, и пункта 10 на python-стороне). Уровень указан; способ выбирает разработчик.

### Шаг 3

1. **Неполная таблица называет диапазон одной формулировкой.** vitest. На `[1..10]` заголовок
   содержит «позиции 1–10 из ТОП-20» и **не** содержит «ТОП-20: позиции». Неверный ответ —
   заголовок с двумя числами подряд.
2. **Полная двадцатка подписана как ТОП-20 без слова «позиции».** vitest, `[1..20]`.
3. **Дырявый набор печатает вычисленный диапазон, а не количество.** vitest, `[4,6,7,8,9,10]` →
   «позиции 4–10 из ТОП-20»; абзац по-прежнему называет пропущенные номера.
4. **Непозиционная таблица не получает ни ТОП-20, ни диапазона.** vitest.

### Шаг 4

5. **Один URL под двумя ref-ами — одна рамка, одно объяснение, одна единица счёта.** vitest. Вход:
   два ref-а с одинаковыми `url`/`title`/`domain` и разными идентификаторами.
6. **Разные URL одного домена остаются двумя строками.** vitest, граница: сведение не должно
   схлопнуть две статьи одного сайта.
7. **Число в подписи равно числу объяснений и числу рамок.** vitest, свойство на наборе с дублями.
8. **Ни один слайд эталона клиентского текста не несёт двух одинаковых буллетов.** vitest поверх
   `fixtures/golden-case/client-text.baseline.json`. При отсутствующем или пустом файле тест
   **падает**, а не пропускается.

### Шаг 5

9. **Заголовок и тело страницы изображений называют одно число.** vitest: одна нарисованная плитка
   негативна по словарю, одна строка без превью → оба числа равны 1.
10. **Негативная строка без превью считается и заголовком, и телом.** vitest, граница: негатив
    только среди непоказанных.
11. **Заголовок «…: биография, бизнес, скандалы» негативен в обоих счётчиках.** vitest, дословный
    вход со стр. 35 — чтобы расхождение словарей не могло вернуться.
12. **Совпадение внутри названия площадки в хвосте заголовка не удваивается доменом.** vitest,
    дословный вход со стр. 62 (`… – The Investigator News`, домен `theinvestigatornews.com`):
    предикат читает заголовок и сниппет, домен не читает; число в заголовке равно числу в теле.
13. **Принадлежность к теме сама по себе строку не красит.** vitest: ref входит в `evidenceRefs`
    находки `medium`, заголовок и сниппет чисты, домен не в списке → не негативна. Это строка
    klerk.ru из аудита.
14. **Благоприятный вердикт не перебивается другим ref-ом того же материала.** vitest: два ref-а
    одного URL, у одного `tone: "supportive"` с цитатой, у другого ничего → строка не негативна.
15. **Вердикт, снятый на одном ref-е, виден всем ref-ам материала.** vitest: вердикт `adverse` с
    тремя цитатами на RU-ref, строка таблицы ОАЭ собрана из своих ref-ов → строка негативна. Это
    `opensanctions.org` из бандла; неверный ответ — сегодняшний «Нейтральный».
16. **Непрочитанная страница не печатается «Нейтральной».** vitest, четыре границы: `readFailure`
    записан → «Не проверено»; вердикта нет вовсе → «Не проверено»; вердикт есть и он `neutral` →
    «Нейтральный»; вердикта нет, но словарь дал негатив → «Нежелательный», а не «Не проверено».
17. **Порядок значений колонки соблюдается.** vitest: строка одновременно `LIKELY_SUBJECT` и без
    вердикта → «Вероятно»; строка `OTHER_SUBJECT` без вердикта → «О другом лице».
18. **Решение по прочитанной странице сильнее словаря в обе стороны.** vitest: `neutral` при слове
    «скандал» в заголовке → не негативна; `adverse` без цитаты → решает словарь.
19. **Мягкий домен не краснеет по словарю, но краснеет по списку площадок.** vitest, границы:
    `ru.wikipedia.org` и **`ru.ruwiki.ru`** со словом «скандал» → не негативны;
    `opensanctions.org` без единого слова словаря → негативна.
20. **Словарь негатива в проекте один.** vitest-сторож по исходнику
    `resolve-observation-highlights.ts`: регулярных выражений по словам негатива в нём не осталось.
21. **«Не проверено» не читается как «Нейтральный».** Проверка на python-стороне
    (`renderer/smoke_search_table_layout.py` или соседний смок): `_status_tone("Не проверено")`
    возвращает янтарный, а не серый цвет «Нейтрального».

### Шаг 6

22. **Тема на странице выдачи названа вместе со своими строками.** vitest: таблица из четырёх
    строк, находка опирается на вторую → абзац содержит и тему, и номер строки.
23. **Тема без опоры среди напечатанных строк не печатается.** vitest, граница: находка попала в
    `view.findings` по URL строки, которой на этой странице нет.
24. **Абзац страницы выдачи по-прежнему не перечисляет домены.** vitest — существующее свойство
    (`serp-page-paragraph-does-not-list-domains.test.ts`) не должно сломаться фразой опоры.
25. **Нейтрально прочитанная страница не цитируется под темой любого уровня риска.** vitest, две
    границы: тема `low` и тема `high` ведут себя одинаково. Это стр. 52.
26. **Счёт материалов темы на региональной странице сходится с её цитатами.** vitest: находка на
    два региона, по одному материалу в каждом → на региональной странице «Всего по теме:
    1 материал».
27. **Тема без пригодных материалов в регионе печатает честную пустую ветку, а не число без
    опоры.** vitest.

### Шаг 7

28. **Запись комплаенса с максимальным числом строк не переполняет лист.** vitest на построителе:
    две записи по 10 строк не ставятся на одну страницу.
29. **Клип на странице комплаенса блокирует выдачу.** vitest на `judgeRenderTelemetry`: строка
    телеметрии с `TABLE_ROW_PARTIALLY_VISIBLE` и именем фигуры комплаенс-страницы даёт блокер.
30. **Клип на прочих страницах остаётся предупреждением.** vitest, граница — иначе шаг превращается
    в остановку здоровых прогонов, против которой прямо возражает
    `render-telemetry-gate.ts:17–28`.

### Смоки

Новых смоков нет: офлайн-контур уже гоняет `golden-case` и `orion-deck-sections`
(`scripts/run-smokes.ts`, тир `offline`); вёрстку проверяют
`scripts/run-orion-deck-sections-report72.ts` и `renderer/smoke_deck_raster_layout.py`.

## Риски и что может сломаться молча

- **Кэш секций.** Без подъёма `DECK_CONTENT_VERSION` правка не доедет ни до новой деки, ни до
  кнопки «Пересобрать отчёт». Бандл это подтверждает: паки прогона собраны `v116`, и исправление,
  вышедшее в `v117`, до этого отчёта не дошло бы вовсе. В шаге 5 сторож отпечатка не увидит
  `load-deck-inputs.ts`.
- **«Не проверено» станет самым частым словом в обоих эталонах.** В фикстурах решений по ссылкам
  нет вовсе (`ENGINEERING.md` §8), поэтому непрочитанной окажется почти каждая строка. Это верно
  по существу, но выглядит как обвал качества. **Объявить владельцу до правки эталона**, иначе
  дифф прочитают неправильно. На живом прогоне доля другая: 90 из 120 страниц прочитаны.
- **Тихое ослабление сигнала.** Снятие `adverseRefSet` и blob-словаря уменьшает число красных
  строк. Поэтому поведения 13, 15 и 19 обязаны быть двусторонними: не только «перестало краснеть»,
  но и «продолжает краснеть по вердикту и по списку площадок».
- **Проверка тем инструментом, который создаёт дефект.** После шага 5 рамка и число станут одним
  выражением; сверять их друг с другом бессмысленно. Настоящая проверка — чтение диффа эталона.
- **Привязка вердикта к материалу может склеить разное.** Ключ `домен|заголовок` устойчив к
  запросу, но два разных URL с одинаковым заголовком на одном сайте склеятся. Поведение 6 сторожит
  именно это.
- **Новое значение колонки и старый рендерер.** Форма пейлоада не меняется — это строка в
  существующей ячейке, — поэтому рендерер прошлой версии страницу нарисует и ничего не потеряет:
  он покрасит «Не проверено» серым вместо янтарного. **Окно деплоя не открывается**, но в отчёте
  это надо назвать: расхождение цвета между сервисами живёт до выката рендерера.
- **Пропажа темы вместо неверной темы.** Шаг 6(а) может убрать тематический вывод со страницы.
  Страница обязана напечатать описание состава, а не остаться с пустым абзацем. Пустой абзац —
  провал, а не «пусто честнее».
- **Перепагинация в обе стороны.** Шаг 4 сокращает объяснения (золотой отчёт может стать короче),
  шаг 6(а) удлиняет абзац, шаг 7 меняет разбиение комплаенса. `--update-baseline` делает зелёным
  любой результат: сравнивать надо страницы, а не коды возврата.
- **Шаг 7 может «починиться сам».** После шага 2 параллельного плана переполнение стр. 69 может
  исчезнуть, и тест 28 пройдёт на неверной причине. Поэтому 28 пишется на синтетической записи с
  максимальным числом строк, а не на данных этого прогона.
- **Статус с двумя смыслами остаётся и после шага 5.** `requireQuotedAdverse`
  (`contracts/link-verdict.ts:194`) понижает нежелательный вывод без цитаты до `neutral` — то есть
  `neutral` значит и «прочитано и нейтрально», и «понижено за отсутствие цитаты», и обе ветки
  гасят словарь. В этом шаге не трогаем, но молчать нельзя.
- **Пересечение с ожидающими планами.** `docs/adr/0033` (подсказки через Топвизор) двинет
  `client-text.baseline.json`; `docs/adr/0032` (выбор персоны) меняет набор запросов — а значит, и
  число наблюдений на материал, то есть прямо влияет на шаги 4 и 5. Закрывать последовательно.

## Глобальное тестирование после шага

После каждого из пяти шагов:

```bash
npm run typecheck
npm run ci
npm run build
npx tsx scripts/run-orion-deck-sections-report72.ts
python3 renderer/smoke_deck_raster_layout.py
```

Наблюдаемые исходы, которых не покрывает автоматика и которые называются словами:

- **шаг 3** — эталоны не изменились байт-в-байт; изменены ровно два ожидания в
  `serp-table-caption-tells-collected-range.test.ts`, и это названо отдельной строкой;
- **шаг 4** — названо, сколько объяснений схлопнулось, стал ли золотой отчёт короче и
  воспроизводится ли дефект на `report-72`;
- **шаг 5** — доля строк «Не проверено» в каждом эталоне названа числом до и после; перечислены
  группы строк, сменивших оценку, с причиной по каждой («перестала краснеть по принадлежности к
  теме», «начала краснеть по слову "скандал"», «стала "Не проверено"»); заголовки всех страниц
  изображений сверены с телом; §8 `ENGINEERING.md` переписан тем же коммитом;
- **шаг 6** — перечислены темы, потерявшие цитату, и темы, получившие номера строк; названа
  страница, где тематический вывод исчез, и сказано, что встало вместо него;
- **шаг 7** — сказано, исчезло ли переполнение стр. 69 само после параллельного шага 2, и показано,
  что ворота срабатывают на синтетической записи.

Живой платный прогон не нужен: все дефекты воспроизводятся на золотом кейсе, `report-72` и
диагностическом бандле.

## Что переедет в ENGINEERING.md

Пункты 1 и 2 — **часть шага 5 и правятся тем же коммитом**: расхождение кода и документа есть баг
одного из двух, и оставлять его на «потом» нельзя. Остальное переезжает при закрытии своего шага.

1. **§8, «"Негативна ли строка" и "почему она выделена" — по одному ответу на отчёт»** (строка
   1776) — **раздел неверен и переписывается.** Написать: ответ один и живёт в `resolveRowAdverse`;
   порядок решений (вердикт с цитатой → список негативных площадок → словарь `adversePatterns` из
   конфига, кроме мягких доменов); словарь негатива один и лежит в конфиге; словарь читает
   заголовок и сниппет, домен отвечает отдельным списком; принадлежность к теме оценкой материала
   не является; **вердикт привязан к материалу, а не к наблюдению** — на прогоне Кремлёва вердикт
   `adverse` с тремя цитатами по `opensanctions.org` не дошёл до строки таблицы ОАЭ именно
   из-за этого; строки запроса (подсказки, связанные) размечаются текстовым словарём, и это другой
   вопрос, а не недоделка.
2. **§8, «Объяснение — на каждую рамку»** (строка 2332) — поправить: правило остаётся, но «рамка» —
   это **материал**, а не наблюдение. Дедупликация по `ref` его не обеспечивала: ключ наблюдения
   включает запрос, и один и тот же URL, найденный двумя запросами, давал две рамки и два
   одинаковых объяснения.
3. **Новый раздел в §8, «Одна строка — один материал»** (шаг 4) — любая поверхность, показывающая
   строки выдачи, обязана сводить их по материалу тем же ключом, что таблица. На прогоне Кремлёва
   снимок ОАЭ насчитал пять выделенных результатов при трёх материалах и выпустил лишнюю страницу.
4. **Новый раздел в §8, «Непрочитанная страница называется словом»** (шаг 5) — 14 строк из 38
   печатались «Нейтральный» при непрочитанной странице; признак берётся из данных (вердикта по
   материалу нет), а не из слова статуса; порядок значений колонки; непрочитанная строка с
   негативным сигналом остаётся «Нежелательной». Там же — отдельной строкой: **разные оценки у
   одной статьи на двух языках дефектом не являются**, если обе страницы прочитаны: es.wikipedia
   получила `neutral`, fr.wikipedia — `adverse` с цитатой про непризнание AIBA, и это два честных
   вердикта по двум разным текстам. Без этой строки кто-нибудь «починит» честную оценку обратно.
5. **§8, «Страница изображений считает всё, что на ней нашли»** (2303) — дописать: заголовок и тело
   считают одним выражением и по одному набору строк; «Показано N» остаётся числом нарисованных.
6. **Новый раздел в §8, «Тема называет, на чём стоит»** (шаг 6) — тема печатается с номерами своих
   строк (домены печатают полосы адресов — решение владельца 25.08); тема без опоры среди
   напечатанных строк не печатается; материал, прочитанный и признанный нейтральным, не цитируется
   доказательством темы **любого** уровня риска; счёт материалов на региональной странице
   пересчитывается вместе с цитатами. Там же — остающийся долг: как материал попадает в корзину
   темы, по бандлу установить не удалось, и омонимия словаря («courts» как «обхаживает») не
   лечится выражением.
7. **§8, рядом с разделами о заголовках** (шаг 3) — заголовок таблицы выдачи называет диапазон один
   раз, формулировка живёт в одной функции, единственный её сторож — юнит: ни золотой кейс, ни
   `report-72` эту ветку не исполняют.
8. **§8, про ворота телеметрии** (шаг 7) — клип остаётся предупреждением везде, кроме
   комплаенс-страницы: там обрезается строка карточки записи, а карточка — печатный носитель
   правила «совпадение уходит аналитику целиком». И: ёмкость страницы комплаенса считается в
   строках, а не в записях, — запись даёт от 6 до 10 строк.
9. **Про сторож версии** — отпечаток следит за построителями; `load-deck-inputs.ts` и
   `finding-synthesizer.ts` в него не входят, а решают текст страницы: правка там требует ручного
   подъёма версии.
10. **Про молчаливую потерю** — измеренный случай: шаблон печатал два предложения склейки, а
    телеметрия рендерера честно отвечала `clipped: false`, потому что до неё доехало уже
    обрезанное. **Телеметрия отвечает за то, что получила, а не за то, что было собрано**;
    проверять потерю надо сравнением пака с текстовым слоем PDF.

---

# Шаг 6, продолжение. Решение аналитика доезжает до деки; абзац страницы выдачи короче

> **Редакция 4 (26.08.2026).** Дописано после третьего круга ревью шага 6. Две работы, обе решены
> владельцем: (А) явное решение аналитика по материалу учитывается тем же единственным предикатом,
> которым дека красит строку выдачи и ставит рамку на снимке; (Б) абзац страницы выдачи сокращается
> так, чтобы кегль 11 pt держали все ячейки решётки «тема × имя», а не восемь из двадцати четырёх.
> Нумерация поведений идёт с **28**, как задано. **Внимание ревьюеру:** номера 28–30 уже заняты
> шагом 7 (комплаенс) в разделе «Что должно быть протестировано» выше; шаг 7 в этом коммите не
> исполняется, и его поведения к приёмке продолжения шага 6 отношения не имеют. Когда речь о
> поведении 28–42 в этом разделе, имеется в виду список **этого** раздела.
>
> **Поправка к условию задачи.** В задании сказано, что в дереве `DECK_CONTENT_VERSION` = v128.
> Замер: в рабочем дереве **v129** (`content-version.ts:16`, файл правлен 26.08 в 22:09, после
> отчёта ревьюера в 21:40), и комментарий версии называет третий круг правок — строгий фильтр
> региона в подвале источников, признак «обвиняет ли тема» в самом каталоге, присказка по разметке
> сборщика. То есть находки Н2, Н3 и Н4 круга 2 в дереве уже закрыты; открытой из круга 2 остаётся
> **Н1**, и она — предмет работы А. В `HEAD` (`22ef8a8`) по-прежнему v126.

## Задача и границы (продолжение шага 6)

В объёме:

- **работа А** — решение аналитика по материалу (`ADVERSE_MEDIA` / `NEUTRAL`) доезжает до слоя деки
  и до слоя рисованных активов и учитывается в `resolveRowAdverse` **первым**, до вердикта
  прочитанной страницы, списка площадок и словаря. Второго предиката не заводится: у существующего
  ответа появляется ещё один источник;
- **работа Б** — предложение о наборе запросов на странице выдачи называет общую часть один раз,
  а не повторяет имя субъекта пять раз. Содержание («по каким именно запросам проверена выдача»)
  сохраняется полностью;
- правка `docs/ENGINEERING.md:2801`, где сейчас написано неверное «Второго ответа на "негативен ли
  материал" в отчёте нет».

Явно **вне** объёма:

- **сведение `itemIsAdverse` и `resolveRowAdverse` в один предикат.** Оно двигает матрицу рисков,
  уровни находок (`riskFor`) и весь исполнительный контур — это отдельный шаг с отдельным диффом
  эталона. Здесь расхождение только **сужается** и **называется словами** в `ENGINEERING.md`;
- расширение или сужение `adversePatterns`, в том числе запрет пускать словарь по `classification`
  и по `sourceUrl` внутри `itemText` (`finding-synthesizer.ts:143`). Это причина оставшейся части
  расхождения, и трогать её в этом шаге нельзя по решению владельца 26.08 (пункт 4 выше);
- подъём потолка вводного абзаца `SEARCH_TABLE_INTRO_MAX_H`. Владелец рассмотрел и отклонил:
  1 400 000 EMU возвращает 11 pt всем 24 ячейкам, но роняет ёмкость таблицы выдачи с 4 пар до 3,
  то есть добавляет по странице к каждой таблице;
- `related.ts:83`, `identity.ts`, `suggestions.ts` — свои предикаты негативности по строкам
  запросов. Не трогаем (граница задана в начале плана);
- мёртвый экспорт `subjectQuerySetClientLine` (`subject-query-set.ts:302`) — вторая клиентская
  формулировка «по каким запросам смотрели», живых потребителей нет (грепом: только
  `tests/unit/subject-query-set.test.ts`). Замечено, в этом шаге не трогаем.

## Контекст (продолжение шага 6)

### А. Где живёт решение аналитика и почему дека его не видит — измерено

**Источник решения.** `applyAnalystOverrides` (`analyst-overrides-loader.ts:347`) на классификацию
`ADVERSE_MEDIA` зовёт `markAdverse` (там же, :326): `item.classification = "adverse_media"` и
`rawMetadata.analystAdverse = true`, противоположный флаг удаляется; на `NEUTRAL` / `POSITIVE` /
`IRRELEVANT` — `markNeutral` (:335) с `analystNeutral = true`. Оба флага читаются готовыми
экспортами `isAnalystAdverse` / `isAnalystNeutral` (:591, :586).

**Кто это уже учитывает.** `itemIsAdverse` (`finding-synthesizer.ts:149`) — первыми двумя строками,
до словаря; `surface-analyzers.ts:54-55`; `run-analytics-pipeline.ts:520-522`. То есть **порядок
«решение аналитика сильнее всего остального» в проекте уже принят в трёх местах** — четвёртый
порядок был бы вторым ответом.

**Аудиторский след.** `run-analytics-pipeline.ts:746` пишет `analytics/analyst-overrides-applied.json`
с записями `{kind, matchKey, inventoryId, effect}` (`analyst-overrides-loader.ts:62-79`).
На золотом прогоне файл содержит ровно три записи, и `inventoryId` заполнен у всех трёх:

```
classification_neutral   searchResult:sr-ru-o-0  obs-deafcc58a694e911  exclude from adverse counts
classification_adverse   searchResult:sr-ru-o-3  obs-a33406d9b1e1737e  classification:=adverse_media theme=adverse_media
identity_other_subject   searchResult:sr-ru-o-4  obs-a6bdca380ecc8b74  decision:=OTHER_SUBJECT
```

**Почему дека этого не видит — по коду, а не по счёту полей.** `loadDeckInputsFromAnalyticsDir`
(`load-deck-inputs.ts:435`) собирает `evidenceIndex` из `composite-serp-observations.json`,
`link-verdicts.json`, `compliance-inventory.json` и проверки Википедии. Ни инвентарь, ни
`analyst-overrides-applied.json` он не читает вовсе, а в типе `ScopedEvidenceIndex`
(`scoped-input.ts:125-330`) поля решения аналитика нет. Замер ревьюера (325 записей индекса, ноль
полей решения) — то же самое с другой стороны.

**Следствия, обе стороны, на золотом кейсе** (проверено по
`fixtures/golden-case/client-text.baseline.json` и артефактам прогона):

| материал | что решил аналитик | что печатает отчёт сейчас |
|---|---|---|
| `obs-deafcc58a694e911`, affarsposten.se, Google RU поз. 1, «…faces tax-fraud probe…» | **NEUTRAL** | таблица `Россия — Google, ТОП-20 (1/5)`, строка 1 — «**Нежелательный**»; снимок выдачи: «выделено результатов повышенного внимания: 3 (affarsposten.se, …)» и объяснение «Потенциально негативные публикации — affarsposten.se» |
| `obs-a33406d9b1e1737e`, finansbladet.se, Google RU поз. 2, «…CEO of Nordkap Capital AB — fintech investor profile» | **ADVERSE_MEDIA** | та же таблица, строка 2 — «Не проверено»; тема «Деловой профиль» на `p07_ru_summary__cont1` — «Всего по теме: 3 материала.» без хвоста |

Дословно из эталона (`slides[19].table.rows`):
`['1', 'Anders Holmström, founder of Nordkap Capital, faces tax-fraud probe in Stockholm', '—', 'Нежелательный']`,
`['2', 'Anders Holmström, CEO of Nordkap Capital AB — fintech investor profile', '—', 'Не проверено']`.

**Насколько эта правка закрывает Н1 — измерено, и ответ «частично».** Ревьюер намерил расхождение
дашборда и региональных страниц в 6 материалов из 27 («PEP» 4, «Офшоры» 1, «Деловой профиль» 1).
Я пересчитал независимо, но по тем полям, которые лежат в артефактах прогона (заголовок и адрес; ни
сниппета, ни `classification` в наблюдениях нет): по заголовку и адресу оба предиката дают **9 = 9**
на всех четырёх находках. Значит, всё расхождение приходит из полей, которых у деки нет:

- «Деловой профиль», 1 материал — **решение аналитика**. Эту единицу правка закрывает;
- «PEP», 4 и «Офшоры», 1 — остальное. Одну из этих единиц я объяснил прямо:
  `inventory:db-golden-ln-am-1` — запись LexisNexis, её сниппет в фикстуре
  (`fixtures/golden-case/build-observations.ts:595`) содержит слова «adverse media and watchlist», а
  индекс деки для комплаенс-записи сниппета не заводит вовсе (обогащение пишет `summary`,
  `load-deck-inputs.ts:713-731`). Остальные четыре я по артефактам не разложил — инвентаря с
  `classification` в прогоне не сохраняется; ревьюер называет причиной словарь, пущенный по
  машинному ярлыку `classification` («adverse_media» совпадает со словом `adverse`, которое стоит в
  `finding-themes.ts:230`). **Это его замер, я его не перепроверял.**

Итог для владельца: после работы А на золотом кейсе сходится «Деловой профиль» (обе страницы —
«3 материала, с негативным контекстом — 1»), «PEP» и «Офшоры» продолжают расходиться, и причина
расхождения переезжает в `ENGINEERING.md` словами вместо сегодняшнего неверного «второго ответа
нет».

### А2. Третье место, где отвечают на тот же вопрос, — рамка на снимке. Измерено

`adverseVisualSidebar` (`shared.ts:3318`) отбирает выделенные строки **не своим** предикатом, а по
флагу `v.adverse` нарисованной строки (`shared.ts:3349`), и доктекст рядом объясняет почему: «считает
их не дека», рамку рисует тот, кто выбирает строки для показа. Флаг ставит
`canonical-visual-assets.ts` — `toVisibleItem` (:183) и сетка изображений (:865) зовут
`classifyObservationHighlight`, а тот отдаёт негативность тому же `resolveRowAdverse`.

**Вход у них — сам `RawInventoryItem`**, то есть ровно тот объект, который `applyAnalystOverrides`
уже пометил флагами; в вызов эти флаги просто не передаются. Порядок вызовов проверен:
`buildCanonicalVisualAssets({ items, … })` (`canonical-report-prepare.ts:1249`) идёт **после**
`runOrionAnalyticsPipeline` (:1217) и получает тот же массив `items`, который правки аналитика
мутировали на месте.

Практический вывод: если донести решение только до индекса деки, отчёт станет противоречить сам
себе на соседних листах золотого кейса — таблица скажет «Не проверено» про affarsposten.se, а через
пять страниц снимок обведёт его красным и объяснит словами «Потенциально негативные публикации».
Поэтому работа А обязана закрыть оба входа, и это не расширение задачи, а её условие.

### Б. Из чего состоит абзац страницы выдачи и где его кромка — измерено

Состав (порядок задаёт `serpTablePageProse`, `serp.ts:292`): лид «Показана выдача *Яндекса* по
запросу «*Имя*».» → «Позиции … отсутствуют…» → вывод по теме с опорой (`themeAttentionLine`,
`shared.ts:901`) → справка о наборе запросов (`subjectQueriesLine`, `shared.ts:2294`) → «почему
важно» → рекомендация.

**Самый дорогой кусок — справка.** Сегодня она печатает каждый запрос целиком:

```
Выдача проверена по 5 запросам: «глинка сергей михайлович», «глинка сергей михайлович отзывы»,
«глинка сергей михайлович компромат», «глинка сергей михайлович суд», «глинка сергей михайлович бизнес».
```

199 знаков, из них имя субъекта — пять раз; шестой раз оно стоит в лиде.

**Такой набор — не выдумка смока.** `subjectQueries` (`shared.ts:2273`) берёт только запросы с
`queryPurpose === "subject_lookup"` (или без назначения вовсе). В плане запросов такие строки — это
`fullName` и подсказки поисковика, причём подсказке без фамилии имя дописывается **в начало**:
«имя субъекта + текст подсказки» (`subject-query-set.ts:258`, `joined`). То есть «имя + слово» и есть
типичный живой набор. Запасная ветка — перестановки ФИО (`orion-query-plan.ts:331`), у них общей
приставки нет.

**Ни один эталон эту фразу не исполняет** — проверено:

- `baselines/report-72/artifacts/analytics/composite-serp-observations.json`: 343 наблюдения,
  `query` и `queryPurpose` равны `null` **у всех**. `subjectQueries` возвращает пустой список,
  справка не печатается. Абзацы страниц выдачи в паке `ru/serp.json` состоят из одного предложения
  вывода по теме;
- золотой кейс: `query` = «Anders Holmström» у 315 наблюдений из 318, то есть запрос **один**, и
  справку гасит защита `repeatsTableQuery` (`serp.ts:313`). В `client-text.baseline.json` строки
  «Выдача проверена по» нет ни одной.

Следствие: **клиентский текст работы Б не двинет ни один эталон**, и единственный сторож формата —
питоновский смок вёрстки плюс юнит, который надо завести (см. поведения 39–41).

**Кромка кегля — замер, воспроизводимый без сборки слайда.** Кегль абзаца выбирает `_Ctx.body`
(`renderer/orion_golden_render/common.py:1090`): 11 pt, если
`measure_text_height(text, CONTENT_W, 11, line_spacing=1.2) <= avail`, иначе следующая ступень шкалы
вниз — а ниже 11 в `TYPE_SCALE_PT` только 9. `avail = min(SEARCH_TABLE_INTRO_MAX_H, …) = 1 000 000`.
Я прогнал эту формулу на решётке смока и получил **все 24 ячейки в точности так, как объявляет
`INTRO_FONT_BY_THEME`** (`smoke_search_table_layout.py:127`) — 8 ячеек на 11 pt, 16 на 9 pt. Модель
воспроизводит замер целиком, поэтому предсказания ниже — не оценка, а тот же прибор.

Отдельно: число знаков кромкой **не является** и предсказателем быть не может — в решётке
620 знаков дают 11 pt («Финансовые претензии» + «Глинка»), а 610 дают 9 pt («Семья и деловые связи»
+ «Глинкин»). Решает ширина, и ширину гонит имя, входящее в абзац шесть раз.

## Решение (продолжение шага 6)

### А1. У решения аналитика — своё поле, у ответа — прежнее место

`AdverseRowInput` (`resolve-observation-highlights.ts:165`) получает пятое поле:

```ts
analystDecision?: "ADVERSE" | "NEUTRAL" | null;
```

Одно поле с двумя значениями, а не два булевых флага: противоречивое состояние «и то, и другое» не
должно существовать по построению (источник его и не создаёт — `markAdverse`/`markNeutral` удаляют
противоположный флаг). Это же и есть «состояние — данные, а не название».

`resolveRowAdverse` (:199) начинается с двух строк **до** всего остального:

```ts
if (row.analystDecision === "NEUTRAL") return false;
if (row.analystDecision === "ADVERSE") return true;
```

**Порядок сил: решение аналитика → вердикт прочитанной страницы → список негативных площадок →
словарь.** Причины, которые обязаны пережить этот файл (в доктексте функции и в `ENGINEERING.md`):

1. вердикт — это модель, прочитавшая страницу, а решение аналитика — человек, посмотревший тот же
   материал и отвечающий за отчёт перед банком. Правило «совпадение не подтверждается
   автоматически» ставит человека выше машины во всём продукте;
2. тот же порядок уже принят в `itemIsAdverse` (`finding-synthesizer.ts:150-152`),
   `surface-analyzers.ts:54-55` и `run-analytics-pipeline.ts:520-522`. Четвёртый порядок был бы
   вторым ответом на вопрос «кто главнее»;
3. список негативных площадок аналитик тоже перебивает — и это не побочный эффект, а тот же
   продуктовый смысл: снять `PENDING` с записи санкционного реестра может только аналитик, и его
   «нет» обязано доезжать до листа;
4. **вердикт `subjectMatch: "other"` решение аналитика не отменяет и не отменяется им**, потому что
   это ответ на другой вопрос. Принадлежность едет своим путём (`subject-resolution.json` →
   `subjectDecision`), у аналитика для неё своя правка (`identity_other_subject`), а на печати
   принадлежность всё равно сильнее: в лестнице оценок строки `other` стоит выше `adverse`
   (`serp.ts:614-624`), и `countsTowardSubjectNegative` не считает чужой материал негативом страницы
   (`shared.ts:1053-1060`). То есть «аналитик первый» и «однофамилец не негатив» не спорят.

**Второго предиката не появляется.** Ответ по-прежнему один и живёт в `resolveRowAdverse`; у него
прибавился источник, а не двойник.

### А2. Как решение попадает в индекс деки

`load-deck-inputs.ts` читает **необязательный** `analytics/analyst-overrides-applied.json` — тем же
приёмом, что `link-verdicts.json` и `analysis-scope.json`: файла нет (реплей старого прогона,
`report-72`) — решений нет, всё работает как раньше. Отсутствие файла **не значит «все нейтральны»**.

Отображение записей:

| `kind` | что ставится | почему |
|---|---|---|
| `classification_adverse` | `analystDecision = "ADVERSE"` | прямое решение |
| `classification_neutral` | `analystDecision = "NEUTRAL"` | прямое решение |
| `manual_review_excluded` | `analystDecision = "NEUTRAL"` | в источнике это тот же `markNeutral` |
| `identity_other_subject`, `manual_review_wrong_subject` | **ничего** | это ответ о принадлежности, и он уже едет через `subject-resolution.json`; дублировать его вторым полем — заводить второй ответ |
| `approved_finding` | **ничего** | решение о находке, а не о строке |

Ключ — `inventory:<inventoryId>`, и решение раскладывается по всем ссылкам своего материала тем же
`evidenceMaterialKey`, каким `applyLinkVerdictsToEvidence` (`load-deck-inputs.ts:195`) раскладывает
вердикт. Причина ровно та же и уже записана в доктексте того места: ключ наблюдения включает запрос,
одна страница по двум запросам — две ссылки, и потребители спрашивают `some(...)` по ссылкам
материала (`serp.ts:582`, `shared.ts:1589`). Без раскладки решение `ADVERSE` сработало бы, а
`NEUTRAL` — нет: одна непомеченная ссылка вернула бы `true` и «или» перекрыло бы решение человека.

Цена раскладки названа честно: ключ — `домен|заголовок`, и две разные страницы одного сайта с
одинаковым заголовком делят решение. Это та же цена, которую проект уже принял для вердикта
(там же оговорено, что цитата по такому ключу **не** едет), и заводить ради решения аналитика третий
ответ на «тот же ли это материал» нельзя.

Группировку ссылок по материалу считает один локальный помощник, которым пользуются обе раскладки —
иначе в одном файле окажется два одинаковых цикла над `evidenceMaterialKey`.

**Поле `adverse` индекса решением аналитика не пишется — ни при каких условиях.** Его читают двое:
`evidenceRowVerdict` (`shared.ts:971`) выводит из него `quoted` — «нежелательный вывод прочитанной
страницы подтверждён дословной цитатой», — и `related.ts:83` (строки запросов, заведомо вне объёма).
Записать туда решение аналитика значит утверждать, что страницу открыли и процитировали, когда её
никто не открывал. Это выдуманный факт, а не удобное переиспользование поля.

### А3. Как решение попадает на рамку снимка

В `canonical-visual-assets.ts` оба вызова `classifyObservationHighlight` (:187 и :865) получают
`analystDecision`, выведенный из самого элемента готовыми экспортами
`isAnalystNeutral` / `isAnalystAdverse` (`analyst-overrides-loader.ts:586, :591`). Новых чтений
артефактов, новых аргументов у `buildCanonicalVisualAssets` и новой плумбинги не заводится: объект
уже на входе.

### Б1. Общая часть набора запросов называется один раз

`subjectQueriesLine` (`shared.ts:2294`) меняет форму предложения, `subjectQueries` (:2273) —
**нет**: список остаётся данными, справка — текстом, и защита `repeatsTableQuery` работает как
работала.

Правило одно и с одним условием: **если один из напечатанных запросов является словесной приставкой
всех остальных**, он печатается целиком один раз, а остальные — только своими хвостами. Иначе
печатается прежний перечень.

- приставка ищется **по словам и только с начала строки**. Подстрочное совпадение запрещено: у
  подсказки «биография глинка сергей михайлович» имя стоит внутри, и «он же с добавлением
  «биография»» описало бы запрос, которого не было;
- база обязана сама быть одним из запросов. Иначе фраза назвала бы запросом строку, которую никто
  не отправлял, — это выдуманный факт, а не сокращение;
- хвосты печатаются все и дословно, число запросов остаётся настоящим.

**Рекомендуемая редакция (Р1)** — 120 знаков против 199, дословно:

```
Выдача проверена по 5 запросам: «глинка сергей михайлович» и он же с добавлением «отзывы», «компромат», «суд», «бизнес».
```

Альтернативы, обе тоже дают 24 ячейки из 24:

```
Р2 (117 зн.): Выдача проверена по 5 запросам: «глинка сергей михайлович» и он же со словами «отзывы», «компромат», «суд», «бизнес».
Р3 (125 зн.): Выдача проверена по 5 запросам: «глинка сергей михайлович», этот же запрос со словами «отзывы», «компромат», «суд», «бизнес».
```

Почему Р1, а не более короткая Р2: хвост подсказки бывает не словом, а сочетанием («бизнес
партнёры»), и «со словами «бизнес партнёры»» читается неверно, тогда как «с добавлением» верно и
для слова, и для сочетания. Три знака разницы кегля не меняют — обе редакции ложатся в то же число
строк.

**Что даёт правка — замер тем же прибором, что и решётка смока:**

| редакция | 11 pt при диапазонной опоре | 11 pt при несворачиваемой опоре «(строки 13, 15, 17, 19)» | худшая ячейка |
|---|---|---|---|
| сейчас | **8 / 24** | 6 / 24 | 665 зн., высота 1 186 891 при потолке 1 000 000 |
| Р1 | **24 / 24** | **24 / 24** | 570 зн., высота 989 076 |
| Р2 | 24 / 24 | 24 / 24 | 567 зн., высота 989 076 |
| Р3 | 24 / 24 | 24 / 24 | 575 зн., высота 989 076 |
| запасная ветка (перестановки ФИО, 3 запроса) | 24 / 24 | — | 564 зн. |

**Остаток, который надо назвать, а не спрятать:** худшая ячейка после правки стоит на 98,9 %
потолка, и запаса в ней — около 32 знаков самой широкой кириллицы (в обычной прозе примерно
45–50 знаков). Кромка не исчезла, она отодвинулась за пределы всей решётки; сторожит её та же
решётка, и она обязана краснеть в обе стороны.

Мера построена так: кегль абзаца = `measure_text_height(текст, CONTENT_W, 11, line_spacing=1.2) <=
1 000 000` (`common.py:1090-1125`, `slides.py:87`), состав абзаца — тот же, что в `live_intro_for`
смока. Проверка модели: на текущей редакции она даёт ровно объявленные `INTRO_FONT_BY_THEME`
значения во всех 24 ячейках. Слайд при этом не собирался — считалась только высота.

### Б2. Почему не потолок короба

Отклонено владельцем 26.08: 1 400 000 EMU действительно возвращает 11 pt всем 24 ячейкам, но бюджет
строк падает с 3 510 000 до 3 110 000 при потребности 3 352 800 — ёмкость таблицы выдачи 4 → 3 пар,
то есть лишняя страница на каждой таблице (`ENGINEERING.md` §8, «ёмкость листа выведена»; замер
ревьюера подтверждает оба числа). Правка текста дешевле правки бюджета.

## План реализации (продолжение шага 6)

Один коммит, красная фаза до кода.

**Красная фаза.** Юниты на поведения 28–41 и решётка смока с ожиданием «все 24 — 11 pt» (поведение 42). Красный лог
дословно в каталог шага **до первой строки продакшн-кода**.

1. **`serp-observation/resolve-observation-highlights.ts`** — поле `analystDecision` в
   `AdverseRowInput`; две строки в начале `resolveRowAdverse`; доктекст функции называет новый
   порядок и все четыре причины из «Решения А1» (это тот текст, который должен пережить удаление
   плана).
2. **`orion-golden/deck-sections/load-deck-inputs.ts`** — чтение необязательного
   `analyst-overrides-applied.json`; отображение `kind` по таблице выше; раскладка по материалу
   общим помощником вместе с `applyLinkVerdictsToEvidence`; экспорт функции раскладки для юнита.
   Готово, когда: файла нет — индекс байт-в-байт прежний; файл есть — решение стоит на всех ссылках
   своего материала и **ни на одной чужой**.
3. **`orion-golden/deck-sections/scoped-input.ts`** — поле `analystDecision` в
   `ScopedEvidenceIndex` с комментарием «почему это не `adverse`».
4. **`orion-golden/deck-sections/fragment-builders/shared.ts`** — `evidenceRowAdverse` (:1000)
   передаёт поле в `resolveRowAdverse`. Больше в предикате ничего не меняется.
5. **`services/canonical-visual-assets.ts`** — оба вызова `classifyObservationHighlight` (:187,
   :865) получают решение аналитика из самого элемента через `isAnalystAdverse` /
   `isAnalystNeutral`. Готово, когда рамка на снимке и оценка в таблице отвечают одинаково.
6. **`orion-golden/deck-sections/fragment-builders/shared.ts`, `subjectQueriesLine` (:2294)** —
   редакция Р1 и правило приставки. `subjectQueries` не трогается.
7. **`renderer/smoke_search_table_layout.py`** — литерал справки в `live_intro_for` приводится к
   новой редакции (зеркало построителя, как уже сделано с опорой); `INTRO_FONT_BY_THEME`
   переписывается замером — все 24 ячейки 11 pt; **Т11е3 переносится с самой дешёвой темы (PEP) на
   самую дорогую ячейку решётки** — «Внимание по линии безопасности / оборонный контур» ×
   «Глинкина Светлана Михайловна» × несворачиваемая опора. Это закрытие остатка находки 2, названного
   ревьюером.
8. **`orion-golden/deck-sections/content-version.ts`** — `DECK_CONTENT_VERSION` **v129 → v130** и
   отпечаток. Подъём обязателен: пакеты в рабочем дереве уже собраны v129, и при неподвижной строке
   ворота соберут эталон из кэша (`section-builders.ts:332-336`), как это случилось во втором круге
   (`REUSED_CACHE 22` при зелёной приёмке). Снаружи это по-прежнему один переход с v126.
9. **`docs/ENGINEERING.md`** — правка §8 по разделу «Что переедет» ниже, **тем же коммитом**:
   расхождение кода и документа есть баг одного из двух.
10. **Эталоны.** `fixtures/golden-case/client-text.baseline.json` — обновляется и **читается
    вслух**; `baselines/report-72/**` — пересобирается воротами и обязан измениться только версией и
    отметками времени (справку о запросах он не исполняет, решений аналитика в нём нет).
    `fixtures/golden-case/baseline.json` (числовой) двигаться не должен: наблюдений шаг не меняет.

Пунктов, меняющих `prisma/schema.prisma`, `config/defaults.ts` или переменные окружения, в этом
шаге **нет**. Миграции нет. Новых переменных окружения нет.

## Что должно быть протестировано (продолжение шага 6)

Всё, кроме пункта 42, обязано проходить **офлайн** — без сети, базы и рендерера. Пункт 42 — смок
уровня `full` (`run-smokes.ts:109`), в `npm run ci` он не входит, поэтому формат справки обязан
иметь и офлайн-сторожа (39–41).

28. **Решение аналитика «нежелательный» доезжает до строки выдачи.** vitest. Запись
    `classification_adverse` с `inventoryId`; заголовок материала не содержит ни одного слова
    словаря, страница не читалась. Ожидание: `evidenceRowAdverse` → `true`, оценка строки —
    «Нежелательный». Неверный ответ выглядит как «Не проверено» — то есть явное решение человека
    молча не доехало.
29. **Решение аналитика «нейтральный» гасит словарную метку.** vitest. Заголовок содержит слово
    словаря («fraud»), страница не читалась, аналитик пометил `NEUTRAL`. Ожидание: предикат `false`,
    оценка — «**Не проверено**», а не «Нейтральный»: страницу никто не открывал, и слово «проверено»
    было бы вторым враньём.
30. **Решение аналитика сильнее вердикта прочитанной страницы — обе стороны.** vitest, две границы:
    (а) вердикт `adverse` с цитатой + аналитик `NEUTRAL` → `false`; (б) вердикт `neutral` + аналитик
    `ADVERSE` → `true`. Без обеих сторон правка выглядит правильной и при потерянной ветке.
31. **Решение аналитика сильнее списка негативных площадок.** vitest, граница: домен
    `opensanctions.org` + аналитик `NEUTRAL` → `false`. Это осознанное следствие, а не побочный
    эффект: снять метку с записи реестра может только человек.
32. **Принадлежность решением о негативе не подменяется.** vitest: запись `identity_other_subject`
    (и `manual_review_wrong_subject`) **не** ставит `analystDecision`. Неверный ответ:
    материал о однофамильце становится «нейтральным материалом субъекта» и перестаёт называться
    «Другой субъект».
33. **Материал без решения аналитика ведёт себя ровно как сегодня.** vitest: на индексе без единой
    записи ответы предиката совпадают с ответами до правки на всём наборе фикстуры.
34. **Отсутствие файла — не решение.** vitest: `loadDeckInputsFromAnalyticsDir` на каталоге без
    `analyst-overrides-applied.json` (случай `report-72`) не бросает и не ставит ни одного поля.
    Неверный ответ: исключение при реплее старого прогона либо «все нейтральны».
35. **Решение раскладывается по всем ссылкам своего материала и только по ним.** vitest, две
    границы: две ссылки с одним `домен|заголовок` — решение на обеих; ссылка того же домена с другим
    заголовком — решения нет. Неверный ответ на второй границе — человек пометил один материал, а
    покрасились два.
36. **Счёт темы на региональной странице учитывает решение аналитика.** vitest: тема из трёх
    материалов региона, один помечен `ADVERSE_MEDIA` → «Всего по теме: 3 материала, с негативным
    контекстом — 1». Это та единица, ради которой Н1 закрывается наполовину.
37. **Рамка на снимке отвечает так же, как оценка в таблице.** vitest на `toVisibleItem` (или на
    том, что строит `visibleItems`): элемент с `analystNeutral` не выделяется, с `analystAdverse` —
    выделяется, при том же заголовке и без вердикта. Неверный ответ — соседние листы отчёта,
    спорящие друг с другом.
38. **Плитка сетки изображений — то же самое.** vitest, вторая точка вызова (`:865`): без неё
    правка закрывает один из двух входов и создаёт ровно то расхождение, которое чинит.
39. **Справка называет все запросы, а общую часть — один раз.** vitest на `subjectQueriesLine`:
    набор «имя», «имя отзывы», «имя компромат» → в строке имя встречается **один** раз, каждый
    хвост — один раз, число запросов настоящее. Неверный ответ: потерянный запрос или число, не
    равное длине набора.
40. **Общая часть не выдумывается.** vitest, три границы: (а) запросы без общей приставки
    (перестановки ФИО) → прежний перечень целиком; (б) имя стоит внутри запроса, а не в начале
    («биография имя») → перечень целиком, никакого «с добавлением «биография»»; (в) база не входит в
    набор сама → перечень целиком. Неверный ответ здесь — напечатанный клиенту запрос, которого
    никто не отправлял.
41. **Единственный запрос справку по-прежнему не печатает.** vitest, существующее поведение
    (`serp.ts:313`) не должно сломаться новой формой; и второе существующее свойство — абзац
    страницы выдачи по-прежнему не перечисляет домены
    (`serp-page-paragraph-does-not-list-domains.test.ts`).
42. **Решётка кеглей: все 24 ячейки «тема × имя» — 11 pt**, и при диапазонной опоре, и при
    несворачиваемой. `renderer/smoke_search_table_layout.py`, Т11е1–Т11е3; Т11е3 гоняется на самой
    дорогой ячейке решётки. Проверка обязана краснеть в обе стороны: и когда формулировка
    подорожала, и когда запас вырос (тогда замер пора переписать).

## Риски и что может сломаться молча (продолжение шага 6)

- **Кэш секций.** Дерево уже несёт v129 и собранные ею пакеты. Без подъёма до v130 ворота деки
  зеленеют на старых пакетах, а `section-build-log.json` честно пишет `REUSED_CACHE 22` — это уже
  происходило в круге 2. Ошибиться безопаснее в сторону подъёма.
- **Один вход из двух.** Если закрыть только индекс деки, отчёт начнёт спорить сам с собой:
  таблица «Не проверено» — снимок красной рамкой. Поведения 37 и 38 существуют ровно против этого.
- **Молчаливое ослабление сигнала.** Правка умеет и снимать негатив (аналитик сказал «нейтрально»),
  и ставить его. Поведения 28–31 обязаны быть двусторонними; односторонний набор пройдёт и при
  случайно потерянной ветке `ADVERSE`.
- **Решение аналитика поверх ключа «домен + заголовок».** Две разные страницы одного сайта с
  одинаковым заголовком разделят решение человека. Цена принята и названа; сторожит граница
  поведения 35.
- **«Не проверено» вместо «Нейтральный» на снятой метке.** Строка, у которой аналитик снял негатив,
  а страницу не читали, обязана называться «Не проверено». Печать «Нейтральный» была бы утверждением
  о проверке, которой не было (поведение 29).
- **Ярлык темы для рамки после решения аналитика.** `classifyObservationHighlight` берёт имя сюжета
  у вердикта, а без него — у справочника `THEME_RULES`; материал, помеченный аналитиком и не
  подходящий ни под одно правило, получит «Потенциально негативные публикации». Это честно, но в
  диффе эталона выглядит как потеря темы — прочитать вслух, не «чинить».
- **Формат справки без офлайн-сторожа.** Смок вёрстки — уровня `full`; на чистой машине его не
  гоняют. Без поведений 39–41 правка формулировки уедет в продукт непроверенной.
- **Правило приставки, применённое подстрокой.** Самый вероятный способ ошибиться в работе Б —
  сравнить `includes` вместо словесной приставки и напечатать клиенту запрос, которого не было.
  Это нарушение правила прослеживаемости, а не косметика.
- **Запас 32 знака.** После правки худшая ячейка стоит на 98,9 % потолка. Любое новое предложение в
  абзаце страницы выдачи снова уронит кегль; решётка это поймает, но только на уровне `full`.
- **Эталон `report-72` не исполняет ни одну из двух правок.** Зелёные ворота по нему **ничего не
  доказывают** ни про решение аналитика (файла правок в его артефактах нет), ни про формат справки
  (`query` = `null` у всех 343 наблюдений). Доказывает только золотой кейс и юниты.
- **Окно деплоя.** Форма пейлоада не меняется: правки живут в тексте и в значениях существующих
  ячеек. Новых ручек рендерера нет, `planDigest` не двигается. Окно не открывается.
- **Расхождение остаётся, и его легко «закрыть» задним числом.** После шага «PEP» и «Офшоры»
  по-прежнему считают негатив по-разному на дашборде и на региональной странице. Соблазн подогнать
  одно число под другое в тексте вместо сведения предикатов — это ровно тот дефект, из которого
  вырос весь план 0035.

## Глобальное тестирование после шага (продолжение шага 6)

```bash
npm run typecheck
npm run ci
npm run build
npx tsx scripts/run-orion-deck-sections-report72.ts
python3 renderer/smoke_search_table_layout.py
python3 renderer/smoke_deck_raster_layout.py
```

Наблюдаемые исходы, которые называются словами, потому что автоматика их не покроет:

- **строка 1 таблицы `Россия — Google, ТОП-20 (1/5)`** (affarsposten.se, «…faces tax-fraud probe…»)
  в `client-text.baseline.json` меняется с «Нежелательный» на «Не проверено», а **строка 2**
  (finansbladet.se, «…fintech investor profile») — с «Не проверено» на «Нежелательный». Обе — прямое
  решение аналитика; если изменилась только одна, работа сделана наполовину;
- **страницы `Россия — снимок выдачи` и `…: почему выделено`**: число выделенных результатов и
  состав доменов пересчитаны, объяснение «Потенциально негативные публикации — affarsposten.se»
  исчезло. Назвать, что встало на его место и не осталась ли страница пустой;
- **`p07_ru_summary__cont1`, тема «Деловой профиль»**: «Всего по теме: 3 материала.» →
  «Всего по теме: 3 материала, с негативным контекстом — 1.» — и это то же число, что печатает
  дашборд рисков. Проговорить, что «PEP» и «Офшоры» по-прежнему расходятся, и назвать числа;
- **числовой эталон `fixtures/golden-case/baseline.json` не двинулся** — иначе шаг тронул
  наблюдения, чего он делать не должен;
- **`baselines/report-72/**` изменился только версией и отметками времени**; `contentHash` пакетов
  секций сверить лично и назвать результат;
- **решётка кеглей**: сказать числом, сколько ячеек 11 pt было и стало (ожидание 8 → 24), и
  подтвердить, что Т11е3 гоняется на самой дорогой ячейке;
- **`docs/ENGINEERING.md` переписан тем же коммитом**, и в отчёте приведён новый текст про два
  предиката дословно.

Живой платный прогон не нужен: обе работы воспроизводятся на золотом кейсе и на юнитах.

## Что переедет в ENGINEERING.md (продолжение шага 6)

1. **§8, строки 2799–2802 — абзац «Хвост негатива считается тем же единственным предикатом».**
   Утверждение «Второго ответа на "негативен ли материал" в отчёте нет» **неверно** и снимается.
   Написать вместо него: ответов два, и они названы. Дека спрашивает `resolveRowAdverse` — порядок
   «решение аналитика → вердикт прочитанной страницы → список негативных площадок → словарь по
   заголовку и сниппету». Синтез находок спрашивает `itemIsAdverse` — те же слова словаря, но по
   тексту «заголовок + сниппет + `classification` + `sourceUrl`», без вердикта и без списка
   площадок. Отсюда исполнительный контур и региональная страница могут дать по одной теме разные
   числа: на золотом кейсе до этого шага — 6 материалов из 27. **Решение аналитика теперь общее для
   обоих ответов** и доезжает до таблицы выдачи, до счёта темы и до рамки на снимке. Остаток
   расхождения — от словаря, пущенного по машинному ярлыку `classification` и по адресу; сведение
   двух предикатов в один — отдельный шаг, потому что двигает уровни находок и матрицу рисков.
2. **§8, новый абзац «Решение аналитика — первый ответ».** Порядок и все четыре причины из
   «Решения А1», включая то, почему `subjectMatch: "other"` с ним не спорит. Отдельной строкой:
   решение доезжает тремя точками — индекс деки (`load-deck-inputs.ts` читает
   `analyst-overrides-applied.json`), рамка снимка и плитка сетки
   (`canonical-visual-assets.ts` берёт флаги прямо с элемента инвентаря). Отдельной строкой:
   `identity_other_subject` в этот ответ **не** отображается, потому что это ответ о
   принадлежности.
3. **§8, раздел «Тема называет, на чём стоит», буллет о бюджете абзаца** (сейчас строки 2745–2766)
   — переписать по факту: имя субъекта входит в абзац **два** раза (лид и база набора запросов), а
   не шесть; справка о наборе называет общую часть один раз и печатает хвосты; решётка «8 тем × 3
   имени» после этого держит 11 pt во всех 24 ячейках, включая несворачиваемую опору; запас худшей
   ячейки — около 32 знаков широкой кириллицы, и он единственный, что осталось от кромки. Там же
   сохранить: потолок короба поднять нельзя (1 400 000 EMU снимает проблему целиком, но роняет
   ёмкость таблицы 4 → 3 пар — решение владельца 26.08).
4. **§8, там же — правило записи набора запросов.** Общая приставка ищется по словам и только с
   начала строки; база обязана сама быть одним из запросов; иначе печатается полный перечень.
   Причина — прослеживаемость: сокращение не вправе назвать запрос, которого не отправляли.
5. **§8, про эталоны** — записать измеренное: справку о наборе запросов не исполняет **ни один**
   эталон (`report-72` — `query: null` у всех 343 наблюдений; золотой кейс — единственный запрос,
   справку гасит защита от повтора), поэтому её единственные сторожа — юнит и решётка смока уровня
   `full`.
6. **§8, пункт 9 списка «про сторож версии»** — поправить: `load-deck-inputs.ts`,
   `scoped-input.ts`, `resolve-observation-highlights.ts` и `config/finding-themes.ts` **уже входят**
   в `EXTRA_SOURCES` отпечатка (`tests/unit/deck-content-version.test.ts:79-129`); вне отпечатка
   остаются `finding-synthesizer.ts` и `canonical-visual-assets.ts` — правка второго меняет рамку на
   снимке и требует ручного подъёма версии.
