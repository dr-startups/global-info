# Шаг 15. Генеральный регрессионный прогон

**Дата:** 26.07.2026
**Субъект:** Дуров Павел Валерьевич, регионы RU + AE
**Кейс:** `DPA-2026-0001` / `cms1kz3s60000qadk59w2fv79`
**Стек:** postgres + renderer + app + отдельный сервис-воркер, все миграции с нуля

Проверяются правки шагов 07, 12–14 и этапов 1–5 шага 13 на живом платном прогоне.

---

## Подготовка

База поднята пустой: применены все 24 миграции, включая
`20260726104500_case_is_fixture` и `20260726120000_open_sanctions_provider`.

```
renderer  ok (LibreOffice доступен)
app       database ok, storage ok, renderer ok
worker    запущен, проверка общего хранилища пройдена
```

---

## A. Что видел пользователь

### A1. Консоль браузера чиста — B6 подтверждён

При открытии кейса единственная запись — 404 на `favicon.ico`. `GET /report`,
отвечавший `410 Gone` при каждом открытии, из клиента убран, и это видно.

### A2. Одно действие, один статус — 11.3 и 11.6 подтверждены

Шапка кейса до запуска: один бейдж («Черновик»), одна кнопка «Собрать данные и
построить отчёт». Легаси-кнопки «Сгенерировать отчёт» нет, хотя
`DIGITAL_PROFILE_LEGACY_REPORT_UI=true` — то есть убрана именно она, а не
скрыта вместе со всей панелью.

### A3. Ожидание названо ожиданием — шаг 14 подтверждён

Во время обогащения Arsenkin (та самая фаза, где раньше вылезала ошибка
«Arsenkin durable poll exceeded 40 attempts» с кнопкой):

> Сбор продолжается: система сама вернётся к работе, вмешательство не требуется.
> Следующая попытка в 26.07.2026, 12:17:44

Рядом: `recovery: blocked (JOB_PROGRESSING)`, главное действие заблокировано,
кода ошибки нет. Кнопки, которую пользователь нажимал по нескольку раз,
не появилось.

---

## B. Сбор

Базовый сбор — все живые провайдеры отработали:

```
REAL_YANDEX_SEARCH          SUCCEEDED
REAL_GOOGLE_SEARCH          SUCCEEDED
REAL_WIKIPEDIA              SUCCEEDED
REAL_ORION_SEARCH_PROFILE   SUCCEEDED   343 результата
REAL_ORION_UAE_INTERNATIONAL SUCCEEDED
REAL_SEARCH_SURFACES        SUCCEEDED
REAL_ORION_GOOGLE_SURFACES  SUCCEEDED
```

К моменту перехода на Arsenkin: 343 результата поиска, 62 подсказки,
16 похожих запросов, 67 изображений, 79 видео, 3 блока знаний, 2 статьи
Википедии, 59 риск-находок.

### OpenSanctions — шаг 04.3 подтверждён на живых данных

Проверка выполнилась **внутри прогона**, без отдельного нажатия:

```
провайдер   OPEN_SANCTIONS
статус      SUCCESS
совпадений  1
```

Запись:

```
Pavel Durov · оценка 100 · уверенность HIGH · статус PENDING
темы: poi; должность: chief executive officer (2013-);
источники: wd_oligarchs, wikidata, ru_billionaires_2021
https://www.opensanctions.org/entities/Q149067/
```

Разбор отработал как задумано: тема `poi` сопоставлена с `WATCHLIST`, источник
записан как `OFFICIAL_API`, **совпадение не подтверждено автоматически**
(`PENDING`). Предупреждение прогона — `compliance-hits-pending-review:OPEN_SANCTIONS:1`,
то есть сверка поставлена в задачи аналитику, а не выдана за факт.

Это санкционным попаданием не является и в отчёте таковым не называется: тема
«лицо, представляющее интерес» из открытых наборов Wikidata.

---

## C. Результат

Прогон дошёл до `REPORT_READY`. Дека — **52 слайда**, PDF и PPTX собраны.

```
базовый сбор        343 результата, 245 поверхностей
Arsenkin            5/5 агентов, 183 наблюдения
composite           499 наблюдений
идентификация       414 о субъекте · 20 вероятно · 48 требуют сверки · 17 другие лица
findings            12 подтверждённых (в первом замере плана было 5)
GPT                 stage1 применён, stage2 применён к 16 фрагментам,
                    0 fallbackError, 0 fallbackValidation
визуальные          13 собрано, 0 отказов
пустых слайдов      6 (все с названной причиной)
```

Время: базовый сбор ~2 мин, обогащение Arsenkin **65 мин** (агенты идут
последовательно), composite + подготовка ~7 мин.

### Что подтвердилось

| Находка | Проверка на прогоне |
|---|---|
| B6 — консоль | При открытии кейса только 404 favicon; `410` от `GET /report` нет |
| B6 — легаси-кнопка | «Сгенерировать отчёт» отсутствует при `LEGACY_REPORT_UI=true` |
| B6.1 — журнал | Одна строка вместо трассы Prisma |
| 11.3 / 11.6 | Один бейдж, одно главное действие |
| 04.3 — OpenSanctions | `SUCCESS`, 1 совпадение, `PENDING`, ссылка на карточку |
| C1 — висящее двоеточие | 0 |
| C2 — подсказка как источник | 0 в основном тексте |
| C5 — дословные повторы | оговорка один раз вместо шести |
| C7 — обрывы в прозе | 0 |
| C10 — сходимость чисел | 414 + 20 + 48 + 17 = 499 ✓ |
| D5 — «продолжение (продолжение)» | 0 |
| 07.5 — мёртвая плитка | «Вероятно о субъекте» = 20 |
| 07.9 — единый вердикт | плашка и текст: «высокий» |
| C13 — пустой комплаенс | Dow Jones и LexisNexis — честное пустое состояние |
| 12 — рестарт воркера | сбор не потерян дважды |

---

## D. Дефекты, найденные прогоном

### D1. Шаг конвейера умирал на ожидании — ИСПРАВЛЕНО

`ARSENKIN_ENRICHMENT`: `40/40`, `STEP_ATTEMPTS_EXCEEDED`, при том что 3 из 5
агентов уже завершились. Бюджет попыток тратился на ожидание, а число
пробуждений зависит от интервала опроса: «40 попыток» при паузе 30 секунд — это
двадцать минут.

Тот же дефект, что нашёл заказчик в UI, уровнем выше. В шаге 13 он был
«исправлен» поднятием чисел — поднятые числа не лечат таймер, притворяющийся
бюджетом повторов.

Теперь у шага два разных бюджета: `maxAttempts` ограничивает **отказы**,
`maxWaitMs` — **ожидание**. Ожидание счётчик попыток не тратит вовсе. Потолок
ожидания Arsenkin совпадает с потолком обогащения: два предела на одно ожидание
противоречили бы друг другу.

### D2. Сигнал продвижения не двигался — ИСПРАВЛЕНО

Замер читал сводное состояние джобы, а оно обновляется только на границах
агентов: пока первый из пяти работает, там нули. Счётчик простоя рос при живом
провайдере (2 задачи `DONE`, замер показывал 0). Сигнал взят из строк в базе —
задачи в `DONE` и сохранённые наблюдения. После правки счётчик упал с 15 до 0.

### D3. Гейт покрытия тем выбросил собранный прогон — ИСПРАВЛЕНО

`MATERIAL_THEME_COVERAGE=87.5`, гейт требует ровно 100. Корень — противоречие
двух множеств: тему объявляла **любая** существенная заявка, а покрыть её могла
только заявка с подтверждённым субъектом. Тема, которую несут лишь
`AMBIGUOUS`-заявки, обязательна и непокрываема одновременно.

Дефект был заложен раньше, но достижимым его сделали правки C9/C10: они
переводят часть заявок из `SUBJECT_MATCH` в `AMBIGUOUS`. Предикат
`canRepresentTheme` теперь один на оба вопроса.

### D4. Внутренний код в клиентской таблице — ИСПРАВЛЕНО

В колонке «База данных» стояло `OPEN_SANCTIONS` — из моей же правки 04.3.
Добавлен ярлык, и закрыт класс: незнакомый код провайдера приводится к
читаемому виду, а не печатается дословно.

**Важное уточнение к шагу 07.8.** Я утверждал, что проверка внутренних кодов
идёт «на каждой сборке деки». Она идёт, но **не блокирует**: отчёт валидации
только пишется в артефакт, и никто в `services/` его не читает. Блокирует
только текст, написанный моделью (`rejectReason`), а детерминированные
построители — нет. Именно поэтому код и прошёл.

### D5. Код отказа висел на успешном прогоне — ИСПРАВЛЕНО

В шапке готового отчёта: `error: CANONICAL_PREPARE_FAILED` и
`MATERIAL_THEME_COVERAGE=87.5` — остатки неудавшейся попытки, которая потом
удалась. При успехе ошибка теперь снимается.

---

## E. Найдено, не исправлено

### E1. Гейт убивает оплаченный сбор и предлагает бесполезную кнопку

При отказе `CANONICAL_PREPARE_FAILED` прогон уходил в `FAILED_TERMINAL` с
полностью собранными данными и **предлагал восстановление**. Повтор
детерминированного гейта над тем же входом помочь не может — это ровно та
кнопка, которая не работает. Корневую причину (D3) я убрал, механизм остался.

### E2. Три одинаковые страницы «связанные запросы (1–3)»

Стр. 31–33 дословно повторяют друг друга: «Поверхность не собиралась в этом
прогоне». Тот же дефект, что четыре страницы «изображения» в шаге 13 (D6):
слоты резервируются независимо от наличия данных.

### E3. Обрывы заголовков в таблицах выдачи

C7 исправил прозу, но ячейки таблиц по-прежнему обрываются с приписанной
точкой: «Как Павел Дуров создал «ВКонтакте», Telegram и стал.»,
«Павел Дуров — биография основателя Telegram.».

### E4. Заголовок обрывается на середине слова

Стр. 9: «Внимание по линии безопасности / оборонный контур — **требует**» —
без «подтверждения».

### E5. Поисковая подсказка цитируется как материал в приложении

Стр. 52: «Криминальные / судебные материалы: “дуров суд сегодня”».
`looksLikeSearchQuery` применяется в композиторе резюме, но не в приложении.

### E6. Тема офшоров опирается на энциклопедическую справку

«Найдены публикации об офшорных и корпоративных структурах владения “Павел
Валерьевич Дуров — российский предприниматель…”». C3 снял суффикс «— Википедия»,
но общая биографическая фраза по-прежнему проходит как доказательство темы.

### E7. Повтор строки о смешении лиц

Стр. 45: «Риск смешения с другим лицом: Дуров, Павел Валерьевич» — три раза
подряд.

### E8. Незакрытые пустые причины в панели качества

`p34_dow_jones — Неизвестная причина: no-compliance-records` — код есть, ярлыка
нет.

### E9. Список предупреждений растёт без границы

368 записей к концу прогона, многие дословные повторы
(`arsenkin-awaiting-ingest` на каждом опросе).

### E10. `AgentRun` для unified-пути не создаётся

`[arsenkin-case-agent] AgentRun … отсутствует — итог прогона записать некуда`.
Итоги агентов Arsenkin не попадают во вкладку «Агенты». Правка B6.1 сделала это
видимым, а не шумным.

### E11. Фикстура смока не подходит

`report-assets.json` этого прогона использует другую схему имён
(`ru_serp_snapshot` против `ru_provider_serp_synserp_…`), поэтому на баз��вых
данных report-72 смок падает. Смок нужно перебазировать на текущий прогон —
это отдельная работа, фикстуру от другого субъекта подкладывать нельзя.
