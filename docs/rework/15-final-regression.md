# Шаг 15. Генеральный регрессионный прогон

**Дата:** 26.07.2026
**Субъект:** Дуров Павел Валерьевич, регионы RU + AE
**Кейс:** `DPA-2026-0001` / `cms1kz3s60000qadk59w2fv79`
**Стек:** postgres + renderer + app + отдельный сервис-воркер, все миграции с нуля

Проверяются правки шагов 07, 12–14 и этапов 1–5 шага 13 на живом платном прогоне.

---

## Подготовка

База поднята пустой: применены все 24 миграции, включая
`20260726104500_case_is_fixture` и `20260726120000_open_sanctions_provider`.

```
renderer  ok (LibreOffice доступен)
app       database ok, storage ok, renderer ok
worker    запущен, проверка общего хранилища пройдена
```

---

## A. Что видел пользователь

### A1. Консоль браузера чиста — B6 подтверждён

При открытии кейса единственная запись — 404 на `favicon.ico`. `GET /report`,
отвечавший `410 Gone` при каждом открытии, из клиента убран, и это видно.

### A2. Одно действие, один статус — 11.3 и 11.6 подтверждены

Шапка кейса до запуска: один бейдж («Черновик»), одна кнопка «Собрать данные и
построить отчёт». Легаси-кнопки «Сгенерировать отчёт» нет, хотя
`DIGITAL_PROFILE_LEGACY_REPORT_UI=true` — то есть убрана именно она, а не
скрыта вместе со всей панелью.

### A3. Ожидание названо ожиданием — шаг 14 подтверждён

Во время обогащения Arsenkin (та самая фаза, где раньше вылезала ошибка
«Arsenkin durable poll exceeded 40 attempts» с кнопкой):

> Сбор продолжается: система сама вернётся к работе, вмешательство не требуется.
> Следующая попытка в 26.07.2026, 12:17:44

Рядом: `recovery: blocked (JOB_PROGRESSING)`, главное действие заблокировано,
кода ошибки нет. Кнопки, которую пользователь нажимал по нескольку раз,
не появилось.

---

## B. Сбор

Базовый сбор — все живые провайдеры отработали:

```
REAL_YANDEX_SEARCH          SUCCEEDED
REAL_GOOGLE_SEARCH          SUCCEEDED
REAL_WIKIPEDIA              SUCCEEDED
REAL_ORION_SEARCH_PROFILE   SUCCEEDED   343 результата
REAL_ORION_UAE_INTERNATIONAL SUCCEEDED
REAL_SEARCH_SURFACES        SUCCEEDED
REAL_ORION_GOOGLE_SURFACES  SUCCEEDED
```

К моменту перехода на Arsenkin: 343 результата поиска, 62 подсказки,
16 похожих запросов, 67 изображений, 79 видео, 3 блока знаний, 2 статьи
Википедии, 59 риск-находок.

### OpenSanctions — шаг 04.3 подтверждён на живых данных

Проверка выполнилась **внутри прогона**, без отдельного нажатия:

```
провайдер   OPEN_SANCTIONS
статус      SUCCESS
совпадений  1
```

Запись:

```
Pavel Durov · оценка 100 · уверенность HIGH · статус PENDING
темы: poi; должность: chief executive officer (2013-);
источники: wd_oligarchs, wikidata, ru_billionaires_2021
https://www.opensanctions.org/entities/Q149067/
```

Разбор отработал как задумано: тема `poi` сопоставлена с `WATCHLIST`, источник
записан как `OFFICIAL_API`, **совпадение не подтверждено автоматически**
(`PENDING`). Предупреждение прогона — `compliance-hits-pending-review:OPEN_SANCTIONS:1`,
то есть сверка поставлена в задачи аналитику, а не выдана за факт.

Это санкционным попаданием не является и в отчёте таковым не называется: тема
«лицо, представляющее интерес» из открытых наборов Wikidata.
