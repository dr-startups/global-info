-- OpenSanctions как источник комплаенса (шаг 04.3).
--
-- Продуктовое решение: подписок на LexisNexis / Dow Jones / World-Check нет, а
-- раздел, наполняемый демо-данными, — ложь в документе о реальном человеке.
-- OpenSanctions закрывает санкционные перечни, списки PEP и розыск законно,
-- открытым API и без закупки.
--
-- Значение добавляется перед OTHER: порядок в enum задаёт порядок сортировки
-- по столбцу, и «прочее» должно оставаться последним.

ALTER TYPE "DatabaseProvider" ADD VALUE IF NOT EXISTS 'OPEN_SANCTIONS' BEFORE 'OTHER';
