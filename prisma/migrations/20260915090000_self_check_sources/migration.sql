-- Какие группы источников ответили в лёгком прогоне. Результат для посетителя
-- хранится в записи проверки, а не читается из джобы: полный прогон из админки
-- заменяет строку джобы дела.
ALTER TABLE "dp_self_checks" ADD COLUMN "sourcesJson" JSONB;
