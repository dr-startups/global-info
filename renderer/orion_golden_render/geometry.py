"""Геометрия мастер-слайда — один ответ на весь рендерер.

Числа жили двумя копиями: в `common.py` (рисование и замер) и в
`deck_raster_layout.py` (растровая проверка), с примечанием «те же значения, что
у рендерера». Шаг 0114 сдвинул нижний запас в `common.py`, копия в растре
осталась прежней, и растровая проверка искала кромку сцены там, где её больше
нет. Модуль — лист без зависимостей: его импортируют оба, и расходиться им
негде. `deck_raster_layout.py` импортируется из `layout_cleeq.py`, поэтому
импортировать `common.py` он не может — а этот модуль может.
"""

# Master slide 16:10 (12.8" × 8.0") — matches ORION reference aspect.
SLIDE_W = 11_704_320
SLIDE_H = 7_315_200
MARGIN_X = 480_000
CONTENT_W = SLIDE_W - 2 * MARGIN_X
FOOTER_Y = SLIDE_H - 440_000
# Нижний запас — 800 000 EMU до края листа при подвале на 440 000: прежние
# 1 100 000 оставляли над подвалом 660 000 EMU (≈ 52 pt) пустоты на каждой
# странице (шаг 0114, решение 4(б) программы 0095). Число одно на замер, на
# вывод и на растровую проверку: ёмкость страницы в TS не объявляется.
CONTENT_BOTTOM = SLIDE_H - 800_000
