/**
 * Значения по умолчанию для настроек, которые не являются секретами.
 *
 * Правило продукта: **в переменных окружения живут только секреты**. Всё
 * остальное — включатели провайдеров, выбор стратегии, пределы и адреса —
 * имеет в коде такое значение, при котором продукт работает целиком. Проект
 * поддерживают разные люди, и требовать от них помнить набор флагов значит
 * заранее согласиться на молча выключенные источники: ровно это и случилось на
 * стенде, где не хватало `GOOGLE_EXTERNAL_SERP_PROVIDER`.
 *
 * Включать провайдеров по умолчанию безопасно, потому что разрешением служит
 * ключ, а не флаг: без ключа провайдер отдаёт `NOT_CONFIGURED` с названной
 * причиной и в сеть не ходит. Флаг сам по себе ничего не открывает и ничего
 * не тратит.
 *
 * Модуль существует, чтобы значение было записано **один раз**. Прежде те же
 * решения принимались и в конфигурации, и в проверке окружения по отдельности,
 * а расходящиеся ответы на один вопрос — главный источник дефектов этого
 * проекта.
 */

/** Настройки-переключатели: имя переменной → значение при её отсутствии. */
export const BOOLEAN_DEFAULTS = {
  /** Модуль включён; выключать его целиком нужно разве что при отладке. */
  DIGITAL_PROFILE_ENABLED: true,
  /** Незакрытая админка — открытый доступ к делам клиентов. */
  DIGITAL_PROFILE_AUTH_ENABLED: true,
  /** Общий рубильник платных SERP-провайдеров. */
  DIGITAL_PROFILE_REAL_CONNECTORS_ENABLED: true,
  DIGITAL_PROFILE_GOOGLE_ENABLED: true,
  DIGITAL_PROFILE_GOOGLE_REAL_ENABLED: true,
  DIGITAL_PROFILE_YANDEX_ENABLED: true,
  DIGITAL_PROFILE_YANDEX_REAL_ENABLED: true,
  DIGITAL_PROFILE_WIKIPEDIA_ENABLED: true,
  /** Пять агентов обогащения; разрешение — `ARSENKIN_API_TOKEN`. */
  ARSENKIN_ENABLED: true,
  /** Без него клиентский текст вырождается в шаблон. */
  DIGITAL_PROFILE_AI_ANALYST_ENABLED: true,
  /** Текущий формат отчёта. */
  DIGITAL_PROFILE_ORION_GOLDEN_ENABLED: true,
  /**
   * Единственный работающий источник комплаенса: без него раздел санкций, PEP
   * и розыска остаётся пустым. Разрешение — адрес и ключ: облачный сервис
   * отвечает по своему адресу, самостоятельно поднятый `yente` — по своему, а
   * ключ обязателен ровно тогда, когда адрес равен облачному умолчанию. Без
   * ключа облако получало ФИО, дату рождения и гражданство субъекта и отвечало
   * 401 — теперь такой источник объявляется ненастроенным до сети.
   */
  OPEN_SANCTIONS_ENABLED: true,
  /** Разбирает очередь ручной проверки сам, не оставляя работу человеку. */
  ORION_GPT_AUTO_ANALYST: true,
  /**
   * Стадия 1.5 над пакетами секций: модель планирует композицию деки. Без
   * слоя GPT у сборки стадия не выполняется вовсе, поэтому разрешением служит
   * ключ OpenAI, а не это значение.
   */
  ORION_GPT_DECK_COMPOSER: true,
  /** Стадия 3: редактор деки. Разрешение — тот же ключ, а не флаг. */
  ORION_GPT_DECK_EDITOR: true,
  /** Демо-агенты: рабочий продукт собирает отчёт настоящими источниками. */
  DIGITAL_PROFILE_MOCK_AGENTS: false,
} as const;

/** Настройки-значения: имя переменной → значение при её отсутствии. */
/**
 * Кто даёт позиции органической выдачи по движку (шаг 0136).
 *
 * У Яндекса это Topvisor: в поле `url` снимка лежит адрес страницы целиком —
 * `https://ru.wikipedia.org/wiki/Мордашов,_Алексей_Александрович`.
 *
 * У Google Topvisor адреса **не отдаёт**. Живой запрос к снимку 20.09.2026
 * вернул `https://en.wikipedia.org` — только origin; `snippet_ext` пуст, а
 * другие имена полей сервис отвергает: `code 2002: Expected value: url,
 * domain, snippet_title, snippet_body, snippet_ext`. По прогону Мордашова путь
 * был у 34 наблюдений из 403, и все 34 — `https://youtube.com/`.
 *
 * Клиенту в колонке «Ссылка» нужен адрес, по которому он откроет материал и
 * убедится сам. Поэтому позиции Google берутся у Serper: он возвращает `link`
 * целиком, а номер позиции мы считаем сами, не доверяя нумерации его страниц.
 *
 * Факт объявлен здесь один раз, потому что его читают двое: стратегия сбора
 * (кого звать) и планировщик снимков Topvisor (какие регионы спрашивать).
 * Двумя ответами они разойдутся в первую же неделю.
 *
 * Google у Topvisor теряет только органику: подсказки и блок вопросов по нему
 * приносит Arsenkin, ИИ-ответы — контур Яндекса.
 */
export const POSITIONAL_SERP_SOURCE: Record<"YANDEX" | "GOOGLE", "topvisor" | "serper"> = {
  YANDEX: "topvisor",
  GOOGLE: "serper",
};

export const STRING_DEFAULTS = {
  /** Стратегия похода в Google. Прежде было `disabled`, и ключ не помогал. */
  GOOGLE_SEARCH_PROVIDER: "external_serp",
  /** Единственная реализованная в этой сборке внешняя выдача. */
  GOOGLE_EXTERNAL_SERP_PROVIDER: "serper",
  /**
   * Откуда берётся выдача: прежним путём или через Topvisor.
   *
   * Один ответ на вопрос для всего конвейера — органика, AI-ответы, подсказки
   * и частота переключаются вместе. Разрешением служит ключ, а не это
   * значение: режим `topvisor` без `TOPVISOR_API_KEY` и `TOPVISOR_USER_ID`
   * объявляет себя ненастроенным и **не откатывается** на прежний путь, иначе
   * на один вопрос было бы два ответа, и второй — невидимый.
   *
   * Умолчание описывает работающий продукт, поэтому пока сбор через Topvisor
   * собран не целиком, оно остаётся прежним; переключается одним коммитом,
   * когда путь закрыт целиком.
   */
  SERP_COLLECTION_PROVIDER: "legacy",
  /**
   * Поверхности подсказок, собираемые Topvisor: ключи регионов аудита через
   * запятую, пустая строка — не собирать вовсе.
   *
   * Умолчание описывает работающий продукт: в отчёте три поверхности подсказок
   * (RU Яндекс, RU Google, ОАЭ Google). **Topvisor собирает одну — Яндекс:**
   * подсказки Google по российским регионам он не отдаёт вовсе (живой прогон
   * 03.09.2026: «Russian regions not available for provider Google to Collect
   * keywords»), а по Дубаю вернул ноль строк. Подсказки Google обеих стран
   * собирает Arsenkin (`arsenkinSuggestEngines`) — решение владельца 03.09.2026.
   * Заказывать у Topvisor Google-Дубай значило бы платить за второй источник
   * того же. **Цена названа здесь, потому что она и есть причина настройки:**
   * 0,90 ₽ за исходную фразу на поверхность.
   */
  TOPVISOR_SUGGEST_REGIONS: "yandex-moscow",
} as const;

/**
 * Стадии конвейера, на которых работает модель OpenAI.
 *
 * Перечисление названо целиком и в одном месте, потому что по нему строятся два
 * ответа сразу: какая модель идёт на стадию и сколько эта стадия стоила. Стадия
 * называется в самом вызове клиента, и вызов без неё не компилируется — иначе
 * модель выбиралась бы молча, а расход не к чему было бы отнести.
 */
export const GPT_STAGES = [
  // Текст, который читает клиент.
  "case_analysis",
  "executive_summary",
  "deck_compose",
  "deck_edit",
  "slide_copy",
  // Извлечение и решения, которые проверяет код.
  "link_verdict",
  "wikipedia_review",
  "fact_extraction",
  "theme_clustering",
  "identity",
  "auto_analyst",
  // Предложения, которые код принимает только при подтверждении.
  "theme_suggestion",
] as const;

export type GptStage = (typeof GPT_STAGES)[number];

/**
 * Какая модель работает на стадии (решение владельца 20.09.2026).
 *
 * До этого модель была одна на всё — `gpt-5.5` из переменной окружения — и
 * стоила дороже любой из нынешних, кроме флагмана. Стадии же разные и по цене
 * входа, и по цене ошибки:
 *
 * - **`gpt-5.6-sol`** пишет текст, который видит клиент. Это прямой преемник
 *   5.5 (дешевле на 20 % по входу и на 33 % по выходу) и единственное место,
 *   где слог решает: на оценке качества документов Sol держит 1748 Elo против
 *   1593 у Terra.
 * - **`gpt-5.6-terra`** отвечает структурой, которую проверяет код: тему
 *   назначают словари, принадлежность клампится, цитату факта код находит в
 *   тексте материала сам. Вход у этих стадий короткий.
 *
 *   **Дословной цитаты длинной страницы Terra не тянет — измерено** (шаг 0120).
 *   Аудит сверяет каждую цитату с тем же текстом, который видела модель, и на
 *   живых прогонах 20.09.2026 снял 11–26 % цитат Terra против 0,7–2,5 % у
 *   прежней `gpt-5.5`; у разбора статьи Википедии — 3 и 4 выдуманных фрагмента
 *   против нуля. Клиенту ложное не доезжает, но цитат в отчёте меньше, а
 *   один-два нежелательных вывода на прогон теряются вместе со снятой цитатой.
 *   Поэтому чтение страниц и разбор статьи ушли на Sol, а извлечение фактов
 *   осталось: там код сверяет цитату сам, и Terra отбросила 0–2 факта против 1.
 * - **`gpt-5.6-luna`** предлагает темы, и предложение принимается, только если
 *   код нашёл слова темы не меньше чем у двух материалов. Luna вчетверо
 *   дешевле Terra, но на длинном контексте теряет точность (41 % против 90 %),
 *   поэтому за пределы этой стадии не выходит.
 *
 * `gpt-6-astra` в таблице нет сознательно: её преимущество над Sol лежит в
 * агентных задачах и работе с компьютером, а вход стоит вдвое дороже нашей
 * прежней модели.
 *
 * Здесь же и цена ошибки: сменить модель стадии — один коммит, который
 * разворачивается пушем. Поэтому таблица живёт в коде, а не в окружении: в
 * окружении по правилу проекта живут только секреты, а модель секретом не
 * является.
 */
export type GptEffort = "low" | "medium" | "high";

/** Медленный тариф: те же токены по цене пакетной обработки. */
export type GptServiceTier = "flex";

export type GptStageSetup = {
  model: string;
  label: string;
  /**
   * Усилие рассуждения. Поднято только там, где модель **пишет текст
   * клиенту** (шаг 0123): у текста слайдов выход 12 057 токенов на 15 вызовов,
   * из них 3 079 уже рассуждение, и удвоение стоит центы. На чтении усилие
   * остаётся низким сознательно: там 176 вызовов на прогон, и каждый лишний
   * токен умножается на это число, а измеренный дефект чтения — выдуманная
   * цитата, то есть точность воспроизведения, а не длина рассуждения.
   */
  effort: GptEffort;
  /**
   * Тариф. `flex` отдаёт те же токены вдвое дешевле за счёт задержек и отказов
   * «нет ёмкости»; отказ не роняет вызов — он немедленно повторяется обычным
   * тарифом. Стоит у чтения страниц: это ровно та работа, для которой тариф
   * назван провайдером (массовое обогащение), и потеря одной страницы из ста
   * двадцати отчёт не ломает.
   */
  tier?: GptServiceTier;
};

export const GPT_STAGE_MODELS: Record<GptStage, GptStageSetup> = {
  case_analysis: { model: "gpt-5.6-sol", label: "Анализ дела", effort: "medium" },
  executive_summary: { model: "gpt-5.6-sol", label: "Резюме для руководства", effort: "medium" },
  deck_compose: { model: "gpt-5.6-sol", label: "Композиция деки", effort: "medium" },
  deck_edit: { model: "gpt-5.6-sol", label: "Редактор деки", effort: "medium" },
  slide_copy: { model: "gpt-5.6-sol", label: "Текст слайдов", effort: "medium" },
  link_verdict: {
    model: "gpt-5.6-sol",
    label: "Чтение страниц выдачи",
    effort: "low",
    tier: "flex",
  },
  wikipedia_review: { model: "gpt-5.6-sol", label: "Разбор статьи Википедии", effort: "low" },
  fact_extraction: { model: "gpt-5.6-terra", label: "Извлечение фактов", effort: "low" },
  theme_clustering: { model: "gpt-5.6-terra", label: "Сведение сюжетов", effort: "low" },
  identity: { model: "gpt-5.6-terra", label: "Разрешение принадлежности", effort: "low" },
  auto_analyst: { model: "gpt-5.6-terra", label: "Авто-аналитик очереди", effort: "low" },
  theme_suggestion: { model: "gpt-5.6-luna", label: "Подсказки тем", effort: "low" },
};

/** Усилие рассуждения стадии. */
export function effortForStage(stage: GptStage): GptEffort {
  return GPT_STAGE_MODELS[stage]?.effort ?? "low";
}

/** Тариф стадии; `undefined` — обычный. */
export function serviceTierForStage(stage: GptStage): GptServiceTier | undefined {
  return GPT_STAGE_MODELS[stage]?.tier;
}

/**
 * Настройки кэша промпта (шаг 0123).
 *
 * Кэш включён у провайдера по умолчанию, и точку разрыва он ставит сам — на
 * живых прогонах она приходилась на конец промпта: 84 940 токенов из 87 790
 * оплачены как **запись** кэша (1,25× обычного входа), а прочитано из кэша
 * ноль. Явный режим выключает неявную точку, а `prompt_cache_breakpoint` на
 * системном блоке кэширует ровно то, что у всех вызовов стадии одинаково.
 * Системные промпты проекта — от 1 228 до 2 833 токенов, то есть выше порога
 * кэширования в 1 024 токена: точка окупается на каждой стадии.
 */
export const GPT_PROMPT_CACHE_OPTIONS = { mode: "explicit", ttl: "30m" } as const;
export const GPT_PROMPT_CACHE_BREAKPOINT = { mode: "explicit" } as const;

/** Модель стадии. Стадии без строки в таблице не существует — она перечислена. */
export function modelForStage(stage: GptStage): string {
  const row = GPT_STAGE_MODELS[stage];
  if (!row) throw new Error(`gpt-stage-without-model: ${stage}`);
  return row.model;
}

/**
 * Дата, на которую сняты цены ниже.
 *
 * Печатается в артефакте учёта рядом с деньгами: цена — не свойство кода, а
 * состояние прайса провайдера, и число без даты через месяц врёт молча. У
 * `gpt-5.6-sol` и `gpt-5.6-terra` цены **акционные до 21.11.2026** — после
 * этой даты таблицу надо снять заново.
 */
export const GPT_PRICE_TABLE_DATE = "2026-09-20";

/**
 * Цена за миллион токенов, доллары (прайс OpenAI на `GPT_PRICE_TABLE_DATE`).
 *
 * `cachedInput` — цена входа, попавшего в кэш промпта; провайдер включает такие
 * токены и в общий счёт входа, поэтому считать их надо вычитанием, а не
 * сложением. Запись кэша стоит дороже обычного входа в `CACHE_WRITE_MULTIPLIER`
 * раз — это тоже прайс, а не наша выдумка.
 *
 * `gpt-5.5` остаётся в таблице, хотя из конвейера ушла: по ней считается, во
 * сколько обходился прежний прогон, и без неё сравнивать было бы не с чем.
 */
export const GPT_MODEL_PRICES: Record<
  string,
  { input: number; cachedInput: number; output: number }
> = {
  "gpt-6-astra": { input: 10, cachedInput: 1, output: 50 },
  "gpt-5.6-sol": { input: 4, cachedInput: 0.4, output: 20 },
  "gpt-5.6-terra": { input: 2, cachedInput: 0.2, output: 12 },
  "gpt-5.6-luna": { input: 0.2, cachedInput: 0.02, output: 1.2 },
  "gpt-5.5": { input: 5, cachedInput: 0.5, output: 30 },
};

/** Наценка за запись входа в кэш промпта (прайс OpenAI для линейки 5.6). */
export const CACHE_WRITE_MULTIPLIER = 1.25;

/** Цена модели или `undefined` — цены может не быть, и тогда счёт честно неизвестен. */
export function priceForModel(
  model: string
): { input: number; cachedInput: number; output: number } | undefined {
  return GPT_MODEL_PRICES[String(model ?? "").trim().toLowerCase()];
}

export type BooleanSettingName = keyof typeof BOOLEAN_DEFAULTS;
export type StringSettingName = keyof typeof STRING_DEFAULTS;

type EnvLike = Record<string, string | undefined>;

const TRUE_WORDS = ["1", "true", "yes", "on"];
const FALSE_WORDS = ["0", "false", "no", "off"];

/**
 * Прочитать переключатель с учётом значения по умолчанию.
 *
 * Непонятое значение читается как значение по умолчанию, а не как «выключено»:
 * опечатка в настройке не должна тихо отключать источник.
 */
export function boolSetting(
  name: BooleanSettingName,
  env: EnvLike = process.env
): boolean {
  const raw = String(env[name] ?? "").trim().toLowerCase();
  if (TRUE_WORDS.includes(raw)) return true;
  if (FALSE_WORDS.includes(raw)) return false;
  return BOOLEAN_DEFAULTS[name];
}

/** Прочитать значение с учётом значения по умолчанию. */
export function stringSetting(
  name: StringSettingName,
  env: EnvLike = process.env
): string {
  const raw = String(env[name] ?? "").trim();
  return raw.length > 0 ? raw.toLowerCase() : STRING_DEFAULTS[name];
}
