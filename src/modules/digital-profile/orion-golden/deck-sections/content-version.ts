/**
 * Версия содержимого деки — ключ кэша секций (шаг 15, E13).
 *
 * Готовые секции переиспользуются, пока совпадают `inputHash`, версия промпта и
 * **эта строка**. Механизм рабочий: подъём версии инвалидирует все пакеты. Но
 * строка задаётся руками, а значит, её можно забыть — и тогда исправление
 * построителя молча не доедет до деки. Это ловушка не только для разработчика:
 * оператор, нажавший «Пересобрать отчёт» после исправления, получит прежний
 * документ и решит, что исправление не работает.
 *
 * Отпечаток ниже закрывает забывчивость: тест сверяет его с фактическим
 * содержимым построителей. Изменили построитель — тест падает и напоминает
 * поднять версию. Забыть больше нельзя, а угадывать «надо ли» не приходится.
 */

export const DECK_CONTENT_VERSION = "deck-sections-v131" as const;

/**
 * Отпечаток исходников `fragment-builders/` на момент последней сверки.
 * Считается тестом `deck-content-version.test.ts`; при расхождении он
 * объясняет, что делать.
 *
 * Расхождение почти всегда значит «построитель изменился» — и тогда поднимается
 * версия. Исключение узкое и объявляется в ревью: в отпечаток входят файлы
 * целиком, а в некоторых из них живёт не только содержимое страниц (например,
 * цикл «сборка → мера → перекладка» в `run-deck-build.ts`). Правка, которая
 * заведомо не может изменить ни одну **собранную** деку, версию не двигает:
 * подъём инвалидировал бы все готовые пакеты и заставил кнопку «Пересобрать
 * отчёт» строить заново то же самое.
 *
 * Хранится строкой, а не вычисляется в рантайме, потому что в собранном образе
 * исходников может не быть, а ключ кэша нужен и там.
 *
 * Отпечаток внутри того же шага снят ещё дважды, по находкам ревью: сначала
 * пустая метка движка стала читаться так же, как её отсутствие, и доктекст
 * функции назвал обоих своих сторожей; потом главная сессия сняла из доктекста
 * ложное «построитель до них не доходит» — построитель доходит, не доходит
 * фикстура второго сторожа. Второй раз правился только комментарий, то есть
 * ни одна собранная дека измениться не может. Номер при этом остаётся v120 —
 * он уже отличается от задеплоенного, а считать редакции внутри шага ему
 * незачем.
 *
 * 27.08 номер двинут до v131, по блокеру ревью: страница «почему выделено»
 * называет основанием рамки того, кто её поставил. Строка, выделенную которую
 * решил аналитик, объяснялась «оценкой по заголовку выдачи» — в заголовке
 * «…fintech investor profile» негатива нет вовсе, и проверяющий в банке, пойдя
 * по единственному предъявленному основанию, не нашёл бы там ничего. Тема
 * материала переехала из заголовка фразы в пояснение: безобидная рубрика первым
 * словом под красной рамкой спорит с самой рамкой. Правка меняет клиентский
 * текст каждой деки, где есть решения аналитика.
 *
 * 26.08 номер двинут до v130 — продолжение того же шага, две работы владельца.
 * Первая: явное решение аналитика по материалу («нежелательный» / «нейтральный»)
 * доезжает до деки и учитывается **первым** в том же единственном предикате
 * `resolveRowAdverse` — до вердикта прочитанной страницы, списка площадок и
 * словаря. Доезжает тремя точками: индекс доказательств, рамка на снимке выдачи
 * и плитка сетки изображений; закрыть только первую значило бы поссорить отчёт
 * с самим собой. Вторая: справка о наборе запросов называет общую часть один
 * раз («…«имя» и он же с добавлением «отзывы», …») — имя субъекта входило в
 * абзац страницы выдачи шесть раз, и лист печатался девяткой при соседних
 * листах деки в одиннадцатом. Обе правки меняют каждую собранную деку, где есть
 * решения аналитика или несколько запросов о субъекте.
 *
 * 26.08 номер двинут до v129 — третий раз внутри того же шага, по находкам
 * третьего круга ревью: подвал источников региональной страницы фильтрует
 * регион так же строго, как блок темы над ним; признак «обвиняет ли тема»
 * переехал в сам каталог тем (файл переопределения теперь несёт его с собой);
 * присказка темы ищется по разметке сборщика, а не сравнением текста. Ни на
 * золотом кейсе, ни на `report-72` содержимое от этого не изменилось (проверено
 * пересборкой: 22 пакета `REGENERATED`, `contentHash` всех двадцати двух прежние),
 * но затронутые ветки собранную деку менять умеют — а промахнуться безопаснее
 * в сторону подъёма. Ни v127, ни v128 не были закоммичены и задеплоены: снаружи
 * это один переход с v126.
 *
 * 26.08 номер двинут до v128 — второй раз внутри шага, по находкам ревью:
 * опора темы печатается диапазоном («строки 17–20» вместо «17, 18, 19 и 20»),
 * присказка «почему это важно» берётся по идентификатору темы, свидетельство
 * без региона в региональный счёт не входит, хвост «с негативным контекстом»
 * вернулся и считается тем же единственным предикатом на обоих путях, защита от
 * нейтрально прочитанной страницы сузилась до обвиняющих тем, а таблица выдачи
 * сводит строки общим `evidenceMaterialKey`. Подъём здесь не формальность:
 * пакеты секций переиспользуются, пока совпадает **эта строка**, и при
 * неподвижной версии приёмка зелено собрала бы эталон из пакетов v127 —
 * журнал сборки честно писал `REUSED_CACHE 22`, а на страницах выдачи стояла
 * прежняя опора перечислением. v127 не была ни закоммичена, ни задеплоена,
 * поэтому лишних инвалидаций второй подъём не стоит.
 *
 * 26.08 версия поднята до v127: тема на странице выдачи называет свою опору —
 * номера строк этого листа, — а тема, которую не поддерживает ни одна
 * напечатанная строка, на листе не называется вовсе. Там же региональный блок
 * темы перестал переносить счёт материалов из глобального утверждения: на
 * странице ОАЭ стояло «Всего по теме: 2 материала» над одной цитатой, потому
 * что вторая опора темы российская. И материал, чью страницу прочитали и
 * признали нейтральной, больше не цитируется доказательством темы **любого**
 * уровня риска — прежде защита работала только от среднего и выше. Правка
 * меняет каждую собранную деку со страницей выдачи или региональным резюме.
 *
 * 26.08 отпечаток снят ещё раз **без** подъёма версии, по находкам второго
 * ревью: доктекст `material-key.ts` (он звал вернуть сведение в деку — ровно то,
 * что дало обе блокирующие находки), комментарий в `serp.ts` и один ассерт в
 * тесте ключа. Ни то, ни другое, ни третье не может изменить ни одну собранную
 * деку — это то самое узкое исключение, а v123 уже отличается от задеплоенного.
 *
 * 26.08 версия поднята до v123, третий раз внутри шага, и это ревью: сведение
 * по материалу **убрано из деки** и оставлено там, где строки выбираются для
 * показа. Разбор `adverseVisualSidebar` обслуживает три поверхности сразу и не
 * знает, чем на каждой рисуется рамка: он склеивал строку Яндекса со строкой
 * Google на одном снимке и две плитки одной статьи на сетке — лист выходил с
 * двумя красными рамками при подписи «выделено красным: 1» и без объяснения под
 * второй. Дека снова объясняет каждую нарисованную строку, а сводят её
 * `selectVisibleObservationsForEngine` и ветка настоящего скриншота. Правка
 * меняет собранные деки, где такие страницы есть, — пакеты v122 переиспользовать
 * нельзя.
 *
 * 26.08 версия поднята до v122: снимок выдачи сводит выделенные строки по
 * материалу тем же ключом, что таблица (`serpMaterialKey`). Один адрес,
 * найденный двумя запросами, приходил в видимые строки двумя ref-ами и давал
 * две рамки, два дословно совпадающих пункта «почему выделено» и лишнюю единицу
 * в подписи «выделено N». Тем же ключом сводит теперь и отбор строк для самой
 * картинки (`selectVisibleObservationsForEngine`): иначе подпись назвала бы
 * меньше выделенного, чем видно рамок на снимке. Правка меняет каждую собранную
 * деку, где снимок показывает один материал дважды, — исключение «отпечаток без
 * версии» здесь не действует. Сам ключ переехал слоем ниже, в
 * `serp-observation/material-key.ts`, и добавлен в список источников отпечатка.
 * Номер внутри шага двинут дважды (v121 → v122): вторая половина правки —
 * отбор строк для картинки — принята после первой пересборки эталона, а пакеты
 * переиспользуются, пока совпадает эта строка. v121 не была ни закоммичена, ни
 * задеплоена, поэтому лишних инвалидаций подъём не стоит, зато эталон собран
 * тем кодом, который уходит в коммит.
 *
 * 26.08 версия поднята до v120: заголовок неполной таблицы выдачи называет
 * глубину одним утверждением — «Россия — Google, позиции 1–8 из ТОП-20» вместо
 * «Россия — Google, ТОП-20: позиции 1–8», где два числа спорили друг с другом.
 * Правка меняет каждую собранную деку, в которой хоть одна таблица выдачи
 * неполна, — исключение «отпечаток без версии» здесь не действует.
 *
 * 26.08 отпечаток сдвинут ещё раз **без** подъёма версии — то самое узкое
 * исключение: в состав отпечатка добавлен `load-deck-inputs.ts` (он решает,
 * какие данные доедут до построителя, ровно как `section-builders.ts`), а в
 * построителе комплаенса правились только комментарий и локальная переменная.
 * Ни то, ни другое не может изменить ни одну **собранную** деку: меняется вход
 * хеша, а не содержимое страниц. Подъём версии здесь обесценил бы готовые
 * пакеты и заставил платить за стадии GPT заново.
 *
 * 26.08 внутри того же шага номер двинут ещё раз, до v119: решение о том, что
 * в колонке «Совпадение по имени» стоит собственное имя записи, а при его
 * отсутствии прочерк, принято **после** первой пересборки эталона. Пакеты
 * секций переиспользуются, пока совпадает эта строка, — при неподвижной версии
 * приёмка зелено собрала бы деку с прежней колонкой. v118 не была ни
 * закоммичена, ни задеплоена, поэтому лишних инвалидаций подъём не стоит.
 *
 * 26.08 версия поднята до v118: карточка комплаенса перестала печатать шкалу
 * совпадения и «уверенность сопоставления», в сводной таблице колонка оценки
 * заменена колонкой «Совпадение по имени» (собственное имя записи или прочерк —
 * имя субъекта туда не подставляется), а первая фраза сводки больше не
 * называет каждую вернувшуюся запись записью о субъекте. Правка меняет каждую
 * собранную деку, где есть хоть одна запись комплаенса, — исключение «отпечаток
 * без версии» здесь не действует.
 *
 * 25.08 версия поднята до v117: абзац находки сравнивает **предложениями**, а не
 * целыми полями, и считает уже сказанным ячейки таблицы своего слайда; абзац
 * страницы выдачи перестал перечислять домены (их печатают полосы адресов под
 * строками). Правка меняет каждую собранную деку.
 *
 * 25.08 версия поднята до v116: адрес материала уехал из колонки таблицы выдачи
 * в полосу под своей строкой (`rowAddresses`), таблица стала четырёхколоночной,
 * заголовок строки режется на 95 знаках, а ёмкость листа выдачи упала с 12 до 4.
 * Правка меняет каждую собранную деку, и исключение «отпечаток без версии»
 * здесь не действует.
 *
 * Внутри шага номер двигался дважды (v115 → v116): ёмкость листа решалась после
 * первой пересборки эталона, а пакеты секций переиспользуются, пока совпадает
 * **эта строка** — при неподвижной версии дека собралась бы прежней ёмкостью.
 * v115 не была ни закоммичена, ни задеплоена, поэтому лишних инвалидаций нет.
 *
 * Отпечаток после этого сдвинут ещё раз **без** подъёма версии, и это то самое
 * исключение: правка была только в комментариях (`template-registry.ts`,
 * `fragment-builders/shared.ts`) — числа обоснования ёмкости заменены на
 * замеренные, с названным правилом извлечения. Ни одна собранная дека от
 * комментария измениться не может, а подъём версии обесценил бы готовые пакеты
 * и заставил бы платить за стадии GPT заново.
 */
export const DECK_BUILDER_FINGERPRINT = "9e188f77df3b049d" as const;
