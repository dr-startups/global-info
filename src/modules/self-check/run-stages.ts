/**
 * Стадии лёгкого прогона словами — одна таблица на проекцию статуса и экран
 * ожидания. Без импортов: её собирает и браузер, иначе у экрана завелась бы
 * своя копия подписей и текущая строка списка разошлась бы со `stageLabel`.
 *
 * Стадий две — столько, сколько различают данные джобы (решение владельца
 * 15.09.2026). Подписи — из утверждённого макета. «AI-анализ» оставлен решением
 * владельца 15.09, хотя GPT в лёгком прогоне не участвует: находки размечает
 * словарь. Тексты ожидания правятся после замера длительности на первом живом
 * прогоне.
 */

export const RUN_STAGE_LABELS = {
  collecting: "Поиск упоминаний и сверка с открытыми источниками и санкционными списками",
  verdict: "AI-анализ размечает находки и формирует результат",
} as const;

export type RunStage = keyof typeof RUN_STAGE_LABELS;

/** Порядок стадий на экране ожидания. */
export const RUN_STAGES: readonly RunStage[] = ["collecting", "verdict"];
