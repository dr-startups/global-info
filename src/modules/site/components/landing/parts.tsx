/**
 * Мелочи, общие у блоков главной.
 *
 * Показание «средний» стоит в трёх местах страницы (шаг 3, пример результата,
 * узел результата в схеме) и везде значит одно: вторая ступень из трёх. Подпись
 * для экранного диктора одна на все три — иначе одна и та же дуга называлась бы
 * по-разному.
 */
export const MEDIUM_METER_LABEL = "Уровень риска: средний, второй из трёх";
